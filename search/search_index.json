{"config":{"lang":["ja"],"separator":"[\\s\\-\u3000\u3001\u3002\uff0c\uff0e]+","pipeline":["stemmer"]},"docs":[{"location":"","title":"UniConnDBBridge","text":"<p>\u3088\u3046\u3053\u305d\uff01 UniConnDBBridge \u306e\u30c9\u30ad\u30e5\u30e1\u30f3\u30c8\u3078\u3002</p> <p>UniConnDBBridge \u306f\u3001\u3055\u307e\u3056\u307e\u306a\u30c7\u30fc\u30bf\u30d9\u30fc\u30b9\u3068\u8a8d\u8a3c\u65b9\u6cd5\u3092\u30b5\u30dd\u30fc\u30c8\u3059\u308b\u3001SQLAlchemy\u30d9\u30fc\u30b9\u306e\u6c4e\u7528\u7684\u306a\u30c7\u30fc\u30bf\u30d9\u30fc\u30b9\u63a5\u7d9a\u7ba1\u7406\u30e9\u30a4\u30d6\u30e9\u30ea\u3067\u3059\u3002</p> <p>\u8a73\u7d30\u306f\u30ea\u30d5\u30a1\u30ec\u30f3\u30b9\u30ac\u30a4\u30c9\u3092\u3054\u89a7\u304f\u3060\u3055\u3044\u3002</p>"},{"location":"reference/","title":"\u30ea\u30d5\u30a1\u30ec\u30f3\u30b9","text":""},{"location":"reference/#uniconndbbridge.core.DatabaseManager","title":"<code>uniconndbbridge.core.DatabaseManager</code>","text":"<p>Synchronous database connection manager.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>class DatabaseManager:\n    \"\"\"Synchronous database connection manager.\"\"\"\n\n    def __init__(\n        self,\n        url: Optional[str] = None,\n        *,\n        config: Optional[Union[DBConfig, Dict[str, Any]]] = None,\n        **kwargs: Any,\n    ):\n        \"\"\"Initialize DatabaseManager.\n\n        Args:\n            url: SQLAlchemy database URL\n            config: Database configuration (DBConfig or dict)\n            **kwargs: Additional engine options\n        \"\"\"\n        self._engine: Optional[Engine] = None\n        self._session_factory: Optional[sessionmaker] = None\n        self._url: Optional[str] = None\n        self._config: Optional[DBConfig] = None\n        self._auth_plugin: Optional[AuthPlugin] = None\n\n        if url and config:\n            raise ConfigurationError(\"Cannot specify both url and config\")\n\n        if not url and not config:\n            raise ConfigurationError(\"Must specify either url or config\")\n\n        if url:\n            self._url = url\n        else:\n            # Convert dict to DBConfig if needed\n            if isinstance(config, dict):\n                config = DBConfig(**config)\n            self._config = config\n\n            # Create auth plugin if specified\n            if config.auth_plugin:\n                self._auth_plugin = create_auth_plugin(\n                    config.auth_plugin, **config.auth_options\n                )\n                # Apply authentication\n                self._config = self._auth_plugin.authenticate(config)\n\n            # Generate URL from config\n            self._url = config.to_url()\n\n        # Store additional engine options\n        self._engine_kwargs = kwargs\n\n        logger.info(\n            f\"Initialized DatabaseManager with URL: {self._mask_url(self._url)}\"\n        )\n\n    def _mask_url(self, url: str) -&gt; str:\n        \"\"\"Mask password in URL for logging.\"\"\"\n        try:\n            parsed = urlparse(url)\n            if parsed.password:\n                masked_netloc = parsed.netloc.replace(f\":{parsed.password}@\", \":***@\")\n                return url.replace(parsed.netloc, masked_netloc)\n        except Exception:\n            pass\n        return url\n\n    @property\n    def engine(self) -&gt; Engine:\n        \"\"\"Get or create the SQLAlchemy engine.\"\"\"\n        if self._engine is None:\n            self._create_engine()\n        return self._engine\n\n    def _create_engine(self) -&gt; None:\n        \"\"\"Create the SQLAlchemy engine.\"\"\"\n        try:\n            # Prepare engine options\n            engine_options = {}\n\n            # Add config-based options\n            if self._config:\n                engine_options.update(self._config.get_engine_options())\n\n            # Add auth plugin options\n            if self._auth_plugin:\n                auth_options = self._auth_plugin.configure_engine_args()\n                engine_options.update(auth_options)\n\n            # Add additional options\n            engine_options.update(self._engine_kwargs)\n\n            # Get final URL from auth plugin if available\n            final_url = self._url\n            if self._auth_plugin:\n                final_url = self._auth_plugin.get_connection_url(self._url)\n\n            logger.debug(f\"Creating engine with options: {engine_options}\")\n            self._engine = create_engine(final_url, **engine_options)\n\n            # Create session factory\n            self._session_factory = sessionmaker(bind=self._engine)\n\n            logger.info(\"Database engine created successfully\")\n\n        except Exception as e:\n            logger.error(f\"Failed to create database engine: {e}\")\n            raise wrap_sqlalchemy_error(e, \"engine creation\")\n\n    def connect_info(self) -&gt; ConnectionInfo:\n        \"\"\"Get connection information from the current engine.\"\"\"\n        engine = self.engine  # This will create engine if needed\n\n        try:\n            url = engine.url\n\n            return ConnectionInfo(\n                dialect=url.drivername.split(\"+\")[0]\n                if \"+\" in url.drivername\n                else url.drivername,\n                driver=url.drivername.split(\"+\")[1]\n                if \"+\" in url.drivername\n                else \"default\",\n                host=url.host or \"localhost\",\n                port=url.port or 0,\n                database=url.database or \"\",\n                user=url.username or \"\",\n                is_async=False,\n                pool_size=getattr(engine.pool, \"size\", lambda: 0)(),\n                max_overflow=getattr(engine.pool, \"max_overflow\", 0),\n                pool_timeout=getattr(engine.pool, \"timeout\", 30),\n                engine_options=dict(engine.pool.get_pool().params)\n                if hasattr(engine.pool, \"get_pool\")\n                else {},\n            )\n        except Exception as e:\n            logger.error(f\"Failed to get connection info: {e}\")\n            raise wrap_sqlalchemy_error(e, \"connection info retrieval\")\n\n    @contextmanager\n    def session(self) -&gt; Generator[Session, None, None]:\n        \"\"\"Get a database session with automatic transaction management.\"\"\"\n        if self._session_factory is None:\n            # Trigger engine creation\n            _ = self.engine\n\n        session = self._session_factory()\n        try:\n            yield session\n            session.commit()\n        except Exception as e:\n            session.rollback()\n            logger.error(f\"Session error, rolling back: {e}\")\n            raise wrap_sqlalchemy_error(e, \"session operation\")\n        finally:\n            session.close()\n\n    @contextmanager\n    def connection(self):\n        \"\"\"Get a raw database connection.\"\"\"\n        conn = self.engine.connect()\n        try:\n            yield conn\n        except Exception as e:\n            logger.error(f\"Connection error: {e}\")\n            raise wrap_sqlalchemy_error(e, \"connection operation\")\n        finally:\n            conn.close()\n\n    def execute(\n        self,\n        statement: Union[str, Executable],\n        parameters: Optional[Dict[str, Any]] = None,\n    ):\n        \"\"\"Execute a SQL statement.\"\"\"\n        try:\n            with self.connection() as conn:\n                if isinstance(statement, str):\n                    statement = text(statement)\n\n                if parameters:\n                    result = conn.execute(statement, parameters)\n                else:\n                    result = conn.execute(statement)\n\n                conn.commit()\n                return result\n\n        except Exception as e:\n            logger.error(f\"Failed to execute statement: {e}\")\n            raise wrap_sqlalchemy_error(e, \"statement execution\")\n\n    def fetch_one(\n        self,\n        statement: Union[str, Executable],\n        parameters: Optional[Dict[str, Any]] = None,\n    ):\n        \"\"\"Execute a statement and fetch one result.\"\"\"\n        result = self.execute(statement, parameters)\n        return result.fetchone()\n\n    def fetch_many(\n        self,\n        statement: Union[str, Executable],\n        size: Optional[int] = None,\n        parameters: Optional[Dict[str, Any]] = None,\n    ):\n        \"\"\"Execute a statement and fetch multiple results.\"\"\"\n        result = self.execute(statement, parameters)\n        if size is not None:\n            return result.fetchmany(size)\n        return result.fetchall()\n\n    def get_table_names(self, schema: Optional[str] = None) -&gt; List[str]:\n        \"\"\"Get list of table names in the database.\"\"\"\n        try:\n            with self.connection() as conn:\n                metadata = MetaData()\n                metadata.reflect(bind=conn, schema=schema)\n                return list(metadata.tables.keys())\n        except Exception as e:\n            logger.error(f\"Failed to get table names: {e}\")\n            raise wrap_sqlalchemy_error(e, \"table name retrieval\")\n\n    def get_column_names(\n        self, table_name: str, schema: Optional[str] = None\n    ) -&gt; List[str]:\n        \"\"\"Get column names for a table.\"\"\"\n        try:\n            with self.connection() as conn:\n                metadata = MetaData()\n                metadata.reflect(bind=conn, schema=schema, only=[table_name])\n\n                full_table_name = f\"{schema}.{table_name}\" if schema else table_name\n                if full_table_name in metadata.tables:\n                    table = metadata.tables[full_table_name]\n                    return [col.name for col in table.columns]\n                else:\n                    return []\n        except Exception as e:\n            logger.error(f\"Failed to get column names for {table_name}: {e}\")\n            raise wrap_sqlalchemy_error(e, \"column name retrieval\")\n\n    def test_connection(self) -&gt; bool:\n        \"\"\"Test the database connection.\"\"\"\n        try:\n            with self.connection() as conn:\n                conn.execute(text(\"SELECT 1\"))\n            logger.info(\"Connection test successful\")\n            return True\n        except Exception as e:\n            logger.warning(f\"Connection test failed: {e}\")\n            return False\n\n    def close(self) -&gt; None:\n        \"\"\"Close the database engine and all connections.\"\"\"\n        if self._engine:\n            self._engine.dispose()\n            self._engine = None\n            self._session_factory = None\n            logger.info(\"Database engine closed\")\n\n    def __enter__(self):\n        \"\"\"Context manager entry.\"\"\"\n        return self\n\n    def __exit__(self, exc_type, exc_val, exc_tb):\n        \"\"\"Context manager exit.\"\"\"\n        self.close()\n\n    @classmethod\n    def from_discovery(cls, **kwargs) -&gt; \"DatabaseManager\":\n        \"\"\"Create DatabaseManager using connection discovery.\n\n        Args:\n            **kwargs: Arguments for discover_connection()\n\n        Returns:\n            DatabaseManager instance\n        \"\"\"\n        from .discovery import ConnectionDiscovery\n\n        url = ConnectionDiscovery.discover_connection(**kwargs)\n        return cls(url=url)\n\n    @classmethod\n    def discover_connection(\n        cls,\n        *,\n        user: str,\n        password: str,\n        host: str = \"localhost\",\n        port: Optional[int] = None,\n        database: Optional[str] = None,\n        dialects: Optional[Sequence[str]] = None,\n        timeout: float = 5.0,\n    ) -&gt; str:\n        \"\"\"Discover and return a working connection URL.\n\n        Args:\n            user: Username\n            password: Password\n            host: Database host\n            port: Database port (optional)\n            database: Database name (optional)\n            dialects: List of dialects to try (optional)\n            timeout: Connection timeout\n\n        Returns:\n            Working connection URL\n        \"\"\"\n        from .discovery import ConnectionDiscovery\n\n        return ConnectionDiscovery.discover_connection(\n            user=user,\n            password=password,\n            host=host,\n            port=port,\n            database=database,\n            dialects=dialects,\n            timeout=timeout,\n        )\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.engine","title":"<code>engine</code>  <code>property</code>","text":"<p>Get or create the SQLAlchemy engine.</p>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.__enter__","title":"<code>__enter__()</code>","text":"<p>Context manager entry.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def __enter__(self):\n    \"\"\"Context manager entry.\"\"\"\n    return self\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.__exit__","title":"<code>__exit__(exc_type, exc_val, exc_tb)</code>","text":"<p>Context manager exit.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def __exit__(self, exc_type, exc_val, exc_tb):\n    \"\"\"Context manager exit.\"\"\"\n    self.close()\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.__init__","title":"<code>__init__(url=None, *, config=None, **kwargs)</code>","text":"<p>Initialize DatabaseManager.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>Optional[str]</code> <p>SQLAlchemy database URL</p> <code>None</code> <code>config</code> <code>Optional[Union[DBConfig, Dict[str, Any]]]</code> <p>Database configuration (DBConfig or dict)</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional engine options</p> <code>{}</code> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def __init__(\n    self,\n    url: Optional[str] = None,\n    *,\n    config: Optional[Union[DBConfig, Dict[str, Any]]] = None,\n    **kwargs: Any,\n):\n    \"\"\"Initialize DatabaseManager.\n\n    Args:\n        url: SQLAlchemy database URL\n        config: Database configuration (DBConfig or dict)\n        **kwargs: Additional engine options\n    \"\"\"\n    self._engine: Optional[Engine] = None\n    self._session_factory: Optional[sessionmaker] = None\n    self._url: Optional[str] = None\n    self._config: Optional[DBConfig] = None\n    self._auth_plugin: Optional[AuthPlugin] = None\n\n    if url and config:\n        raise ConfigurationError(\"Cannot specify both url and config\")\n\n    if not url and not config:\n        raise ConfigurationError(\"Must specify either url or config\")\n\n    if url:\n        self._url = url\n    else:\n        # Convert dict to DBConfig if needed\n        if isinstance(config, dict):\n            config = DBConfig(**config)\n        self._config = config\n\n        # Create auth plugin if specified\n        if config.auth_plugin:\n            self._auth_plugin = create_auth_plugin(\n                config.auth_plugin, **config.auth_options\n            )\n            # Apply authentication\n            self._config = self._auth_plugin.authenticate(config)\n\n        # Generate URL from config\n        self._url = config.to_url()\n\n    # Store additional engine options\n    self._engine_kwargs = kwargs\n\n    logger.info(\n        f\"Initialized DatabaseManager with URL: {self._mask_url(self._url)}\"\n    )\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.close","title":"<code>close()</code>","text":"<p>Close the database engine and all connections.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def close(self) -&gt; None:\n    \"\"\"Close the database engine and all connections.\"\"\"\n    if self._engine:\n        self._engine.dispose()\n        self._engine = None\n        self._session_factory = None\n        logger.info(\"Database engine closed\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.connect_info","title":"<code>connect_info()</code>","text":"<p>Get connection information from the current engine.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def connect_info(self) -&gt; ConnectionInfo:\n    \"\"\"Get connection information from the current engine.\"\"\"\n    engine = self.engine  # This will create engine if needed\n\n    try:\n        url = engine.url\n\n        return ConnectionInfo(\n            dialect=url.drivername.split(\"+\")[0]\n            if \"+\" in url.drivername\n            else url.drivername,\n            driver=url.drivername.split(\"+\")[1]\n            if \"+\" in url.drivername\n            else \"default\",\n            host=url.host or \"localhost\",\n            port=url.port or 0,\n            database=url.database or \"\",\n            user=url.username or \"\",\n            is_async=False,\n            pool_size=getattr(engine.pool, \"size\", lambda: 0)(),\n            max_overflow=getattr(engine.pool, \"max_overflow\", 0),\n            pool_timeout=getattr(engine.pool, \"timeout\", 30),\n            engine_options=dict(engine.pool.get_pool().params)\n            if hasattr(engine.pool, \"get_pool\")\n            else {},\n        )\n    except Exception as e:\n        logger.error(f\"Failed to get connection info: {e}\")\n        raise wrap_sqlalchemy_error(e, \"connection info retrieval\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.connection","title":"<code>connection()</code>","text":"<p>Get a raw database connection.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>@contextmanager\ndef connection(self):\n    \"\"\"Get a raw database connection.\"\"\"\n    conn = self.engine.connect()\n    try:\n        yield conn\n    except Exception as e:\n        logger.error(f\"Connection error: {e}\")\n        raise wrap_sqlalchemy_error(e, \"connection operation\")\n    finally:\n        conn.close()\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.discover_connection","title":"<code>discover_connection(*, user, password, host='localhost', port=None, database=None, dialects=None, timeout=5.0)</code>  <code>classmethod</code>","text":"<p>Discover and return a working connection URL.</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>Username</p> required <code>password</code> <code>str</code> <p>Password</p> required <code>host</code> <code>str</code> <p>Database host</p> <code>'localhost'</code> <code>port</code> <code>Optional[int]</code> <p>Database port (optional)</p> <code>None</code> <code>database</code> <code>Optional[str]</code> <p>Database name (optional)</p> <code>None</code> <code>dialects</code> <code>Optional[Sequence[str]]</code> <p>List of dialects to try (optional)</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Connection timeout</p> <code>5.0</code> <p>Returns:</p> Type Description <code>str</code> <p>Working connection URL</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>@classmethod\ndef discover_connection(\n    cls,\n    *,\n    user: str,\n    password: str,\n    host: str = \"localhost\",\n    port: Optional[int] = None,\n    database: Optional[str] = None,\n    dialects: Optional[Sequence[str]] = None,\n    timeout: float = 5.0,\n) -&gt; str:\n    \"\"\"Discover and return a working connection URL.\n\n    Args:\n        user: Username\n        password: Password\n        host: Database host\n        port: Database port (optional)\n        database: Database name (optional)\n        dialects: List of dialects to try (optional)\n        timeout: Connection timeout\n\n    Returns:\n        Working connection URL\n    \"\"\"\n    from .discovery import ConnectionDiscovery\n\n    return ConnectionDiscovery.discover_connection(\n        user=user,\n        password=password,\n        host=host,\n        port=port,\n        database=database,\n        dialects=dialects,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.execute","title":"<code>execute(statement, parameters=None)</code>","text":"<p>Execute a SQL statement.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def execute(\n    self,\n    statement: Union[str, Executable],\n    parameters: Optional[Dict[str, Any]] = None,\n):\n    \"\"\"Execute a SQL statement.\"\"\"\n    try:\n        with self.connection() as conn:\n            if isinstance(statement, str):\n                statement = text(statement)\n\n            if parameters:\n                result = conn.execute(statement, parameters)\n            else:\n                result = conn.execute(statement)\n\n            conn.commit()\n            return result\n\n    except Exception as e:\n        logger.error(f\"Failed to execute statement: {e}\")\n        raise wrap_sqlalchemy_error(e, \"statement execution\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.fetch_many","title":"<code>fetch_many(statement, size=None, parameters=None)</code>","text":"<p>Execute a statement and fetch multiple results.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def fetch_many(\n    self,\n    statement: Union[str, Executable],\n    size: Optional[int] = None,\n    parameters: Optional[Dict[str, Any]] = None,\n):\n    \"\"\"Execute a statement and fetch multiple results.\"\"\"\n    result = self.execute(statement, parameters)\n    if size is not None:\n        return result.fetchmany(size)\n    return result.fetchall()\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.fetch_one","title":"<code>fetch_one(statement, parameters=None)</code>","text":"<p>Execute a statement and fetch one result.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def fetch_one(\n    self,\n    statement: Union[str, Executable],\n    parameters: Optional[Dict[str, Any]] = None,\n):\n    \"\"\"Execute a statement and fetch one result.\"\"\"\n    result = self.execute(statement, parameters)\n    return result.fetchone()\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.from_discovery","title":"<code>from_discovery(**kwargs)</code>  <code>classmethod</code>","text":"<p>Create DatabaseManager using connection discovery.</p> <p>Parameters:</p> Name Type Description Default <code>**kwargs</code> <p>Arguments for discover_connection()</p> <code>{}</code> <p>Returns:</p> Type Description <code>DatabaseManager</code> <p>DatabaseManager instance</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>@classmethod\ndef from_discovery(cls, **kwargs) -&gt; \"DatabaseManager\":\n    \"\"\"Create DatabaseManager using connection discovery.\n\n    Args:\n        **kwargs: Arguments for discover_connection()\n\n    Returns:\n        DatabaseManager instance\n    \"\"\"\n    from .discovery import ConnectionDiscovery\n\n    url = ConnectionDiscovery.discover_connection(**kwargs)\n    return cls(url=url)\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.get_column_names","title":"<code>get_column_names(table_name, schema=None)</code>","text":"<p>Get column names for a table.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def get_column_names(\n    self, table_name: str, schema: Optional[str] = None\n) -&gt; List[str]:\n    \"\"\"Get column names for a table.\"\"\"\n    try:\n        with self.connection() as conn:\n            metadata = MetaData()\n            metadata.reflect(bind=conn, schema=schema, only=[table_name])\n\n            full_table_name = f\"{schema}.{table_name}\" if schema else table_name\n            if full_table_name in metadata.tables:\n                table = metadata.tables[full_table_name]\n                return [col.name for col in table.columns]\n            else:\n                return []\n    except Exception as e:\n        logger.error(f\"Failed to get column names for {table_name}: {e}\")\n        raise wrap_sqlalchemy_error(e, \"column name retrieval\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.get_table_names","title":"<code>get_table_names(schema=None)</code>","text":"<p>Get list of table names in the database.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def get_table_names(self, schema: Optional[str] = None) -&gt; List[str]:\n    \"\"\"Get list of table names in the database.\"\"\"\n    try:\n        with self.connection() as conn:\n            metadata = MetaData()\n            metadata.reflect(bind=conn, schema=schema)\n            return list(metadata.tables.keys())\n    except Exception as e:\n        logger.error(f\"Failed to get table names: {e}\")\n        raise wrap_sqlalchemy_error(e, \"table name retrieval\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.session","title":"<code>session()</code>","text":"<p>Get a database session with automatic transaction management.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>@contextmanager\ndef session(self) -&gt; Generator[Session, None, None]:\n    \"\"\"Get a database session with automatic transaction management.\"\"\"\n    if self._session_factory is None:\n        # Trigger engine creation\n        _ = self.engine\n\n    session = self._session_factory()\n    try:\n        yield session\n        session.commit()\n    except Exception as e:\n        session.rollback()\n        logger.error(f\"Session error, rolling back: {e}\")\n        raise wrap_sqlalchemy_error(e, \"session operation\")\n    finally:\n        session.close()\n</code></pre>"},{"location":"reference/#uniconndbbridge.core.DatabaseManager.test_connection","title":"<code>test_connection()</code>","text":"<p>Test the database connection.</p> Source code in <code>uniconndbbridge/core.py</code> <pre><code>def test_connection(self) -&gt; bool:\n    \"\"\"Test the database connection.\"\"\"\n    try:\n        with self.connection() as conn:\n            conn.execute(text(\"SELECT 1\"))\n        logger.info(\"Connection test successful\")\n        return True\n    except Exception as e:\n        logger.warning(f\"Connection test failed: {e}\")\n        return False\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager","title":"<code>uniconndbbridge.async_.AsyncDatabaseManager</code>","text":"<p>Asynchronous database connection manager.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>class AsyncDatabaseManager:\n    \"\"\"Asynchronous database connection manager.\"\"\"\n\n    def __init__(\n        self,\n        url: Optional[str] = None,\n        *,\n        config: Optional[Union[DBConfig, Dict[str, Any]]] = None,\n        **kwargs: Any,\n    ):\n        \"\"\"Initialize AsyncDatabaseManager.\n\n        Args:\n            url: SQLAlchemy database URL\n            config: Database configuration (DBConfig or dict)\n            **kwargs: Additional engine options\n        \"\"\"\n        if not ASYNC_AVAILABLE:\n            raise ConfigurationError(\n                \"Async functionality not available. Install SQLAlchemy with async support: \"\n                \"pip install sqlalchemy[asyncio]\"\n            )\n\n        self._engine: Optional[AsyncEngine] = None\n        self._session_factory: Optional[async_sessionmaker] = None\n        self._url: Optional[str] = None\n        self._config: Optional[DBConfig] = None\n        self._auth_plugin: Optional[AuthPlugin] = None\n\n        if url and config:\n            raise ConfigurationError(\"Cannot specify both url and config\")\n\n        if not url and not config:\n            raise ConfigurationError(\"Must specify either url or config\")\n\n        if url:\n            self._url = self._ensure_async_url(url)\n        else:\n            # Convert dict to DBConfig if needed\n            if isinstance(config, dict):\n                config = DBConfig(**config)\n            self._config = config\n\n            # Create auth plugin if specified\n            if config.auth_plugin:\n                self._auth_plugin = create_auth_plugin(\n                    config.auth_plugin, **config.auth_options\n                )\n                # Apply authentication\n                self._config = self._auth_plugin.authenticate(config)\n\n            # Generate URL from config and ensure it's async\n            base_url = config.to_url()\n            self._url = self._ensure_async_url(base_url)\n\n        # Store additional engine options\n        self._engine_kwargs = kwargs\n\n        logger.info(\n            f\"Initialized AsyncDatabaseManager with URL: {self._mask_url(self._url)}\"\n        )\n\n    def _ensure_async_url(self, url: str) -&gt; str:\n        \"\"\"Ensure URL is properly formatted for async use.\"\"\"\n        parsed = urlparse(url)\n        scheme = parsed.scheme.lower()\n\n        # Map sync schemes to async schemes\n        async_mapping = {\n            \"postgresql\": \"postgresql+asyncpg\",\n            \"mysql\": \"mysql+aiomysql\",\n            \"sqlite\": \"sqlite+aiosqlite\",\n            \"oracle\": \"oracle+oracledb\",  # oracledb supports async\n            \"mssql\": \"mssql+aioodbc\",\n        }\n\n        # If already has a driver specified, check if it's async\n        if \"+\" in scheme:\n            dialect, driver = scheme.split(\"+\", 1)\n            dialect_info = DriverRegistry.get_dialect_info(dialect)\n\n            if dialect_info and driver in dialect_info.async_drivers:\n                # Already async\n                return url\n            elif dialect_info:\n                # Use best async driver for this dialect\n                try:\n                    best_async_driver = DriverRegistry.get_best_driver(\n                        dialect, is_async=True\n                    )\n                    new_scheme = f\"{dialect}+{best_async_driver}\"\n                    return url.replace(scheme, new_scheme, 1)\n                except DriverNotFoundError:\n                    raise ConfigurationError(f\"No async driver available for {dialect}\")\n        else:\n            # No driver specified, use default async mapping\n            if scheme in async_mapping:\n                return url.replace(scheme, async_mapping[scheme], 1)\n\n        # If we can't convert, try to use as-is and let SQLAlchemy handle it\n        return url\n\n    def _mask_url(self, url: str) -&gt; str:\n        \"\"\"Mask password in URL for logging.\"\"\"\n        try:\n            parsed = urlparse(url)\n            if parsed.password:\n                masked_netloc = parsed.netloc.replace(f\":{parsed.password}@\", \":***@\")\n                return url.replace(parsed.netloc, masked_netloc)\n        except Exception:\n            pass\n        return url\n\n    @property\n    def engine(self) -&gt; AsyncEngine:\n        \"\"\"Get or create the async SQLAlchemy engine.\"\"\"\n        if self._engine is None:\n            raise RuntimeError(\n                \"Engine not created. Call create_engine() first or use async context manager.\"\n            )\n        return self._engine\n\n    async def create_engine(self) -&gt; None:\n        \"\"\"Create the async SQLAlchemy engine.\"\"\"\n        try:\n            # Prepare engine options\n            engine_options = {}\n\n            # Add config-based options\n            if self._config:\n                engine_options.update(self._config.get_engine_options())\n\n            # Add auth plugin options\n            if self._auth_plugin:\n                auth_options = await self._auth_plugin.configure_engine_args_async()\n                engine_options.update(auth_options)\n\n            # Add additional options\n            engine_options.update(self._engine_kwargs)\n\n            # Get final URL from auth plugin if available\n            final_url = self._url\n            if self._auth_plugin:\n                final_url = self._auth_plugin.get_connection_url(self._url)\n\n            logger.debug(f\"Creating async engine with options: {engine_options}\")\n            self._engine = create_async_engine(final_url, **engine_options)\n\n            # Create session factory\n            self._session_factory = async_sessionmaker(bind=self._engine)\n\n            logger.info(\"Async database engine created successfully\")\n\n        except Exception as e:\n            logger.error(f\"Failed to create async database engine: {e}\")\n            raise wrap_sqlalchemy_error(e, \"async engine creation\")\n\n    async def connect_info_async(self) -&gt; ConnectionInfo:\n        \"\"\"Get connection information from the current engine.\"\"\"\n        if self._engine is None:\n            await self.create_engine()\n\n        try:\n            url = self._engine.url\n\n            return ConnectionInfo(\n                dialect=url.drivername.split(\"+\")[0]\n                if \"+\" in url.drivername\n                else url.drivername,\n                driver=url.drivername.split(\"+\")[1]\n                if \"+\" in url.drivername\n                else \"default\",\n                host=url.host or \"localhost\",\n                port=url.port or 0,\n                database=url.database or \"\",\n                user=url.username or \"\",\n                is_async=True,\n                pool_size=getattr(self._engine.pool, \"size\", lambda: 0)(),\n                max_overflow=getattr(self._engine.pool, \"max_overflow\", 0),\n                pool_timeout=getattr(self._engine.pool, \"timeout\", 30),\n                engine_options={},\n            )\n        except Exception as e:\n            logger.error(f\"Failed to get async connection info: {e}\")\n            raise wrap_sqlalchemy_error(e, \"async connection info retrieval\")\n\n    @asynccontextmanager\n    async def session(self) -&gt; AsyncGenerator[AsyncSession, None]:\n        \"\"\"Get an async database session with automatic transaction management.\"\"\"\n        if self._session_factory is None:\n            await self.create_engine()\n\n        session = self._session_factory()\n        try:\n            yield session\n            await session.commit()\n        except Exception as e:\n            await session.rollback()\n            logger.error(f\"Async session error, rolling back: {e}\")\n            raise wrap_sqlalchemy_error(e, \"async session operation\")\n        finally:\n            await session.close()\n\n    @asynccontextmanager\n    async def connection(self):\n        \"\"\"Get a raw async database connection.\"\"\"\n        if self._engine is None:\n            await self.create_engine()\n\n        conn = await self._engine.connect()\n        try:\n            yield conn\n        except Exception as e:\n            logger.error(f\"Async connection error: {e}\")\n            raise wrap_sqlalchemy_error(e, \"async connection operation\")\n        finally:\n            await conn.close()\n\n    async def execute(\n        self,\n        statement: Union[str, Executable],\n        parameters: Optional[Dict[str, Any]] = None,\n    ):\n        \"\"\"Execute a SQL statement asynchronously.\"\"\"\n        try:\n            async with self.connection() as conn:\n                if isinstance(statement, str):\n                    statement = text(statement)\n\n                if parameters:\n                    result = await conn.execute(statement, parameters)\n                else:\n                    result = await conn.execute(statement)\n\n                await conn.commit()\n                return result\n\n        except Exception as e:\n            logger.error(f\"Failed to execute async statement: {e}\")\n            raise wrap_sqlalchemy_error(e, \"async statement execution\")\n\n    async def fetch_one(\n        self,\n        statement: Union[str, Executable],\n        parameters: Optional[Dict[str, Any]] = None,\n    ):\n        \"\"\"Execute a statement and fetch one result asynchronously.\"\"\"\n        result = await self.execute(statement, parameters)\n        return result.fetchone()\n\n    async def fetch_many(\n        self,\n        statement: Union[str, Executable],\n        size: Optional[int] = None,\n        parameters: Optional[Dict[str, Any]] = None,\n    ):\n        \"\"\"Execute a statement and fetch multiple results asynchronously.\"\"\"\n        result = await self.execute(statement, parameters)\n        if size is not None:\n            return result.fetchmany(size)\n        return result.fetchall()\n\n    async def get_table_names(self, schema: Optional[str] = None) -&gt; List[str]:\n        \"\"\"Get list of table names in the database asynchronously.\"\"\"\n        try:\n            async with self.connection() as conn:\n                # Use database-specific queries for better async support\n                if self._engine.dialect.name == \"postgresql\":\n                    query = text(\"\"\"\n                        SELECT table_name \n                        FROM information_schema.tables \n                        WHERE table_schema = COALESCE(:schema, 'public')\n                        AND table_type = 'BASE TABLE'\n                    \"\"\")\n                    result = await conn.execute(query, {\"schema\": schema})\n                elif self._engine.dialect.name == \"mysql\":\n                    query = text(\"\"\"\n                        SELECT table_name \n                        FROM information_schema.tables \n                        WHERE table_schema = COALESCE(:schema, DATABASE())\n                        AND table_type = 'BASE TABLE'\n                    \"\"\")\n                    result = await conn.execute(query, {\"schema\": schema})\n                elif self._engine.dialect.name == \"sqlite\":\n                    query = text(\"\"\"\n                        SELECT name FROM sqlite_master \n                        WHERE type='table' AND name NOT LIKE 'sqlite_%'\n                    \"\"\")\n                    result = await conn.execute(query)\n                else:\n                    # Fallback to reflection (may not work well with async)\n                    metadata = MetaData()\n                    await conn.run_sync(metadata.reflect, schema=schema)\n                    return list(metadata.tables.keys())\n\n                return [row[0] for row in result.fetchall()]\n\n        except Exception as e:\n            logger.error(f\"Failed to get table names async: {e}\")\n            raise wrap_sqlalchemy_error(e, \"async table name retrieval\")\n\n    async def get_column_names(\n        self, table_name: str, schema: Optional[str] = None\n    ) -&gt; List[str]:\n        \"\"\"Get column names for a table asynchronously.\"\"\"\n        try:\n            async with self.connection() as conn:\n                # Use database-specific queries for better async support\n                if self._engine.dialect.name == \"postgresql\":\n                    query = text(\"\"\"\n                        SELECT column_name \n                        FROM information_schema.columns \n                        WHERE table_name = :table_name \n                        AND table_schema = COALESCE(:schema, 'public')\n                        ORDER BY ordinal_position\n                    \"\"\")\n                    result = await conn.execute(\n                        query, {\"table_name\": table_name, \"schema\": schema}\n                    )\n                elif self._engine.dialect.name == \"mysql\":\n                    query = text(\"\"\"\n                        SELECT column_name \n                        FROM information_schema.columns \n                        WHERE table_name = :table_name \n                        AND table_schema = COALESCE(:schema, DATABASE())\n                        ORDER BY ordinal_position\n                    \"\"\")\n                    result = await conn.execute(\n                        query, {\"table_name\": table_name, \"schema\": schema}\n                    )\n                elif self._engine.dialect.name == \"sqlite\":\n                    query = text(f\"PRAGMA table_info({table_name})\")\n                    result = await conn.execute(query)\n                    return [\n                        row[1] for row in result.fetchall()\n                    ]  # Column name is index 1\n                else:\n                    # Fallback to reflection\n                    metadata = MetaData()\n                    await conn.run_sync(\n                        metadata.reflect, schema=schema, only=[table_name]\n                    )\n                    full_table_name = f\"{schema}.{table_name}\" if schema else table_name\n                    if full_table_name in metadata.tables:\n                        table = metadata.tables[full_table_name]\n                        return [col.name for col in table.columns]\n                    else:\n                        return []\n\n                return [row[0] for row in result.fetchall()]\n\n        except Exception as e:\n            logger.error(f\"Failed to get column names for {table_name} async: {e}\")\n            raise wrap_sqlalchemy_error(e, \"async column name retrieval\")\n\n    async def test_connection(self) -&gt; bool:\n        \"\"\"Test the database connection asynchronously.\"\"\"\n        try:\n            async with self.connection() as conn:\n                await conn.execute(text(\"SELECT 1\"))\n            logger.info(\"Async connection test successful\")\n            return True\n        except Exception as e:\n            logger.warning(f\"Async connection test failed: {e}\")\n            return False\n\n    async def close(self) -&gt; None:\n        \"\"\"Close the database engine and all connections.\"\"\"\n        if self._engine:\n            await self._engine.dispose()\n            self._engine = None\n            self._session_factory = None\n            logger.info(\"Async database engine closed\")\n\n    async def __aenter__(self):\n        \"\"\"Async context manager entry.\"\"\"\n        await self.create_engine()\n        return self\n\n    async def __aexit__(self, exc_type, exc_val, exc_tb):\n        \"\"\"Async context manager exit.\"\"\"\n        await self.close()\n\n    @classmethod\n    async def from_discovery(cls, **kwargs) -&gt; \"AsyncDatabaseManager\":\n        \"\"\"Create AsyncDatabaseManager using async connection discovery.\n\n        Args:\n            **kwargs: Arguments for discover_connection_async()\n\n        Returns:\n            AsyncDatabaseManager instance\n        \"\"\"\n        from .discovery import ConnectionDiscovery\n\n        url = await ConnectionDiscovery.discover_connection_async(**kwargs)\n        return cls(url=url)\n\n    @classmethod\n    async def discover_connection_async(\n        cls,\n        *,\n        user: str,\n        password: str,\n        host: str = \"localhost\",\n        port: Optional[int] = None,\n        database: Optional[str] = None,\n        dialects: Optional[Sequence[str]] = None,\n        timeout: float = 5.0,\n    ) -&gt; str:\n        \"\"\"Discover and return a working async connection URL.\n\n        Args:\n            user: Username\n            password: Password\n            host: Database host\n            port: Database port (optional)\n            database: Database name (optional)\n            dialects: List of dialects to try (optional)\n            timeout: Connection timeout\n\n        Returns:\n            Working async connection URL\n        \"\"\"\n        from .discovery import ConnectionDiscovery\n\n        return await ConnectionDiscovery.discover_connection_async(\n            user=user,\n            password=password,\n            host=host,\n            port=port,\n            database=database,\n            dialects=dialects,\n            timeout=timeout,\n        )\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.engine","title":"<code>engine</code>  <code>property</code>","text":"<p>Get or create the async SQLAlchemy engine.</p>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.__aenter__","title":"<code>__aenter__()</code>  <code>async</code>","text":"<p>Async context manager entry.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def __aenter__(self):\n    \"\"\"Async context manager entry.\"\"\"\n    await self.create_engine()\n    return self\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.__aexit__","title":"<code>__aexit__(exc_type, exc_val, exc_tb)</code>  <code>async</code>","text":"<p>Async context manager exit.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def __aexit__(self, exc_type, exc_val, exc_tb):\n    \"\"\"Async context manager exit.\"\"\"\n    await self.close()\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.__init__","title":"<code>__init__(url=None, *, config=None, **kwargs)</code>","text":"<p>Initialize AsyncDatabaseManager.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>Optional[str]</code> <p>SQLAlchemy database URL</p> <code>None</code> <code>config</code> <code>Optional[Union[DBConfig, Dict[str, Any]]]</code> <p>Database configuration (DBConfig or dict)</p> <code>None</code> <code>**kwargs</code> <code>Any</code> <p>Additional engine options</p> <code>{}</code> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>def __init__(\n    self,\n    url: Optional[str] = None,\n    *,\n    config: Optional[Union[DBConfig, Dict[str, Any]]] = None,\n    **kwargs: Any,\n):\n    \"\"\"Initialize AsyncDatabaseManager.\n\n    Args:\n        url: SQLAlchemy database URL\n        config: Database configuration (DBConfig or dict)\n        **kwargs: Additional engine options\n    \"\"\"\n    if not ASYNC_AVAILABLE:\n        raise ConfigurationError(\n            \"Async functionality not available. Install SQLAlchemy with async support: \"\n            \"pip install sqlalchemy[asyncio]\"\n        )\n\n    self._engine: Optional[AsyncEngine] = None\n    self._session_factory: Optional[async_sessionmaker] = None\n    self._url: Optional[str] = None\n    self._config: Optional[DBConfig] = None\n    self._auth_plugin: Optional[AuthPlugin] = None\n\n    if url and config:\n        raise ConfigurationError(\"Cannot specify both url and config\")\n\n    if not url and not config:\n        raise ConfigurationError(\"Must specify either url or config\")\n\n    if url:\n        self._url = self._ensure_async_url(url)\n    else:\n        # Convert dict to DBConfig if needed\n        if isinstance(config, dict):\n            config = DBConfig(**config)\n        self._config = config\n\n        # Create auth plugin if specified\n        if config.auth_plugin:\n            self._auth_plugin = create_auth_plugin(\n                config.auth_plugin, **config.auth_options\n            )\n            # Apply authentication\n            self._config = self._auth_plugin.authenticate(config)\n\n        # Generate URL from config and ensure it's async\n        base_url = config.to_url()\n        self._url = self._ensure_async_url(base_url)\n\n    # Store additional engine options\n    self._engine_kwargs = kwargs\n\n    logger.info(\n        f\"Initialized AsyncDatabaseManager with URL: {self._mask_url(self._url)}\"\n    )\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.close","title":"<code>close()</code>  <code>async</code>","text":"<p>Close the database engine and all connections.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def close(self) -&gt; None:\n    \"\"\"Close the database engine and all connections.\"\"\"\n    if self._engine:\n        await self._engine.dispose()\n        self._engine = None\n        self._session_factory = None\n        logger.info(\"Async database engine closed\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.connect_info_async","title":"<code>connect_info_async()</code>  <code>async</code>","text":"<p>Get connection information from the current engine.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def connect_info_async(self) -&gt; ConnectionInfo:\n    \"\"\"Get connection information from the current engine.\"\"\"\n    if self._engine is None:\n        await self.create_engine()\n\n    try:\n        url = self._engine.url\n\n        return ConnectionInfo(\n            dialect=url.drivername.split(\"+\")[0]\n            if \"+\" in url.drivername\n            else url.drivername,\n            driver=url.drivername.split(\"+\")[1]\n            if \"+\" in url.drivername\n            else \"default\",\n            host=url.host or \"localhost\",\n            port=url.port or 0,\n            database=url.database or \"\",\n            user=url.username or \"\",\n            is_async=True,\n            pool_size=getattr(self._engine.pool, \"size\", lambda: 0)(),\n            max_overflow=getattr(self._engine.pool, \"max_overflow\", 0),\n            pool_timeout=getattr(self._engine.pool, \"timeout\", 30),\n            engine_options={},\n        )\n    except Exception as e:\n        logger.error(f\"Failed to get async connection info: {e}\")\n        raise wrap_sqlalchemy_error(e, \"async connection info retrieval\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.connection","title":"<code>connection()</code>  <code>async</code>","text":"<p>Get a raw async database connection.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>@asynccontextmanager\nasync def connection(self):\n    \"\"\"Get a raw async database connection.\"\"\"\n    if self._engine is None:\n        await self.create_engine()\n\n    conn = await self._engine.connect()\n    try:\n        yield conn\n    except Exception as e:\n        logger.error(f\"Async connection error: {e}\")\n        raise wrap_sqlalchemy_error(e, \"async connection operation\")\n    finally:\n        await conn.close()\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.create_engine","title":"<code>create_engine()</code>  <code>async</code>","text":"<p>Create the async SQLAlchemy engine.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def create_engine(self) -&gt; None:\n    \"\"\"Create the async SQLAlchemy engine.\"\"\"\n    try:\n        # Prepare engine options\n        engine_options = {}\n\n        # Add config-based options\n        if self._config:\n            engine_options.update(self._config.get_engine_options())\n\n        # Add auth plugin options\n        if self._auth_plugin:\n            auth_options = await self._auth_plugin.configure_engine_args_async()\n            engine_options.update(auth_options)\n\n        # Add additional options\n        engine_options.update(self._engine_kwargs)\n\n        # Get final URL from auth plugin if available\n        final_url = self._url\n        if self._auth_plugin:\n            final_url = self._auth_plugin.get_connection_url(self._url)\n\n        logger.debug(f\"Creating async engine with options: {engine_options}\")\n        self._engine = create_async_engine(final_url, **engine_options)\n\n        # Create session factory\n        self._session_factory = async_sessionmaker(bind=self._engine)\n\n        logger.info(\"Async database engine created successfully\")\n\n    except Exception as e:\n        logger.error(f\"Failed to create async database engine: {e}\")\n        raise wrap_sqlalchemy_error(e, \"async engine creation\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.discover_connection_async","title":"<code>discover_connection_async(*, user, password, host='localhost', port=None, database=None, dialects=None, timeout=5.0)</code>  <code>async</code> <code>classmethod</code>","text":"<p>Discover and return a working async connection URL.</p> <p>Parameters:</p> Name Type Description Default <code>user</code> <code>str</code> <p>Username</p> required <code>password</code> <code>str</code> <p>Password</p> required <code>host</code> <code>str</code> <p>Database host</p> <code>'localhost'</code> <code>port</code> <code>Optional[int]</code> <p>Database port (optional)</p> <code>None</code> <code>database</code> <code>Optional[str]</code> <p>Database name (optional)</p> <code>None</code> <code>dialects</code> <code>Optional[Sequence[str]]</code> <p>List of dialects to try (optional)</p> <code>None</code> <code>timeout</code> <code>float</code> <p>Connection timeout</p> <code>5.0</code> <p>Returns:</p> Type Description <code>str</code> <p>Working async connection URL</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>@classmethod\nasync def discover_connection_async(\n    cls,\n    *,\n    user: str,\n    password: str,\n    host: str = \"localhost\",\n    port: Optional[int] = None,\n    database: Optional[str] = None,\n    dialects: Optional[Sequence[str]] = None,\n    timeout: float = 5.0,\n) -&gt; str:\n    \"\"\"Discover and return a working async connection URL.\n\n    Args:\n        user: Username\n        password: Password\n        host: Database host\n        port: Database port (optional)\n        database: Database name (optional)\n        dialects: List of dialects to try (optional)\n        timeout: Connection timeout\n\n    Returns:\n        Working async connection URL\n    \"\"\"\n    from .discovery import ConnectionDiscovery\n\n    return await ConnectionDiscovery.discover_connection_async(\n        user=user,\n        password=password,\n        host=host,\n        port=port,\n        database=database,\n        dialects=dialects,\n        timeout=timeout,\n    )\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.execute","title":"<code>execute(statement, parameters=None)</code>  <code>async</code>","text":"<p>Execute a SQL statement asynchronously.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def execute(\n    self,\n    statement: Union[str, Executable],\n    parameters: Optional[Dict[str, Any]] = None,\n):\n    \"\"\"Execute a SQL statement asynchronously.\"\"\"\n    try:\n        async with self.connection() as conn:\n            if isinstance(statement, str):\n                statement = text(statement)\n\n            if parameters:\n                result = await conn.execute(statement, parameters)\n            else:\n                result = await conn.execute(statement)\n\n            await conn.commit()\n            return result\n\n    except Exception as e:\n        logger.error(f\"Failed to execute async statement: {e}\")\n        raise wrap_sqlalchemy_error(e, \"async statement execution\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.fetch_many","title":"<code>fetch_many(statement, size=None, parameters=None)</code>  <code>async</code>","text":"<p>Execute a statement and fetch multiple results asynchronously.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def fetch_many(\n    self,\n    statement: Union[str, Executable],\n    size: Optional[int] = None,\n    parameters: Optional[Dict[str, Any]] = None,\n):\n    \"\"\"Execute a statement and fetch multiple results asynchronously.\"\"\"\n    result = await self.execute(statement, parameters)\n    if size is not None:\n        return result.fetchmany(size)\n    return result.fetchall()\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.fetch_one","title":"<code>fetch_one(statement, parameters=None)</code>  <code>async</code>","text":"<p>Execute a statement and fetch one result asynchronously.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def fetch_one(\n    self,\n    statement: Union[str, Executable],\n    parameters: Optional[Dict[str, Any]] = None,\n):\n    \"\"\"Execute a statement and fetch one result asynchronously.\"\"\"\n    result = await self.execute(statement, parameters)\n    return result.fetchone()\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.from_discovery","title":"<code>from_discovery(**kwargs)</code>  <code>async</code> <code>classmethod</code>","text":"<p>Create AsyncDatabaseManager using async connection discovery.</p> <p>Parameters:</p> Name Type Description Default <code>**kwargs</code> <p>Arguments for discover_connection_async()</p> <code>{}</code> <p>Returns:</p> Type Description <code>AsyncDatabaseManager</code> <p>AsyncDatabaseManager instance</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>@classmethod\nasync def from_discovery(cls, **kwargs) -&gt; \"AsyncDatabaseManager\":\n    \"\"\"Create AsyncDatabaseManager using async connection discovery.\n\n    Args:\n        **kwargs: Arguments for discover_connection_async()\n\n    Returns:\n        AsyncDatabaseManager instance\n    \"\"\"\n    from .discovery import ConnectionDiscovery\n\n    url = await ConnectionDiscovery.discover_connection_async(**kwargs)\n    return cls(url=url)\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.get_column_names","title":"<code>get_column_names(table_name, schema=None)</code>  <code>async</code>","text":"<p>Get column names for a table asynchronously.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def get_column_names(\n    self, table_name: str, schema: Optional[str] = None\n) -&gt; List[str]:\n    \"\"\"Get column names for a table asynchronously.\"\"\"\n    try:\n        async with self.connection() as conn:\n            # Use database-specific queries for better async support\n            if self._engine.dialect.name == \"postgresql\":\n                query = text(\"\"\"\n                    SELECT column_name \n                    FROM information_schema.columns \n                    WHERE table_name = :table_name \n                    AND table_schema = COALESCE(:schema, 'public')\n                    ORDER BY ordinal_position\n                \"\"\")\n                result = await conn.execute(\n                    query, {\"table_name\": table_name, \"schema\": schema}\n                )\n            elif self._engine.dialect.name == \"mysql\":\n                query = text(\"\"\"\n                    SELECT column_name \n                    FROM information_schema.columns \n                    WHERE table_name = :table_name \n                    AND table_schema = COALESCE(:schema, DATABASE())\n                    ORDER BY ordinal_position\n                \"\"\")\n                result = await conn.execute(\n                    query, {\"table_name\": table_name, \"schema\": schema}\n                )\n            elif self._engine.dialect.name == \"sqlite\":\n                query = text(f\"PRAGMA table_info({table_name})\")\n                result = await conn.execute(query)\n                return [\n                    row[1] for row in result.fetchall()\n                ]  # Column name is index 1\n            else:\n                # Fallback to reflection\n                metadata = MetaData()\n                await conn.run_sync(\n                    metadata.reflect, schema=schema, only=[table_name]\n                )\n                full_table_name = f\"{schema}.{table_name}\" if schema else table_name\n                if full_table_name in metadata.tables:\n                    table = metadata.tables[full_table_name]\n                    return [col.name for col in table.columns]\n                else:\n                    return []\n\n            return [row[0] for row in result.fetchall()]\n\n    except Exception as e:\n        logger.error(f\"Failed to get column names for {table_name} async: {e}\")\n        raise wrap_sqlalchemy_error(e, \"async column name retrieval\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.get_table_names","title":"<code>get_table_names(schema=None)</code>  <code>async</code>","text":"<p>Get list of table names in the database asynchronously.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def get_table_names(self, schema: Optional[str] = None) -&gt; List[str]:\n    \"\"\"Get list of table names in the database asynchronously.\"\"\"\n    try:\n        async with self.connection() as conn:\n            # Use database-specific queries for better async support\n            if self._engine.dialect.name == \"postgresql\":\n                query = text(\"\"\"\n                    SELECT table_name \n                    FROM information_schema.tables \n                    WHERE table_schema = COALESCE(:schema, 'public')\n                    AND table_type = 'BASE TABLE'\n                \"\"\")\n                result = await conn.execute(query, {\"schema\": schema})\n            elif self._engine.dialect.name == \"mysql\":\n                query = text(\"\"\"\n                    SELECT table_name \n                    FROM information_schema.tables \n                    WHERE table_schema = COALESCE(:schema, DATABASE())\n                    AND table_type = 'BASE TABLE'\n                \"\"\")\n                result = await conn.execute(query, {\"schema\": schema})\n            elif self._engine.dialect.name == \"sqlite\":\n                query = text(\"\"\"\n                    SELECT name FROM sqlite_master \n                    WHERE type='table' AND name NOT LIKE 'sqlite_%'\n                \"\"\")\n                result = await conn.execute(query)\n            else:\n                # Fallback to reflection (may not work well with async)\n                metadata = MetaData()\n                await conn.run_sync(metadata.reflect, schema=schema)\n                return list(metadata.tables.keys())\n\n            return [row[0] for row in result.fetchall()]\n\n    except Exception as e:\n        logger.error(f\"Failed to get table names async: {e}\")\n        raise wrap_sqlalchemy_error(e, \"async table name retrieval\")\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.session","title":"<code>session()</code>  <code>async</code>","text":"<p>Get an async database session with automatic transaction management.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>@asynccontextmanager\nasync def session(self) -&gt; AsyncGenerator[AsyncSession, None]:\n    \"\"\"Get an async database session with automatic transaction management.\"\"\"\n    if self._session_factory is None:\n        await self.create_engine()\n\n    session = self._session_factory()\n    try:\n        yield session\n        await session.commit()\n    except Exception as e:\n        await session.rollback()\n        logger.error(f\"Async session error, rolling back: {e}\")\n        raise wrap_sqlalchemy_error(e, \"async session operation\")\n    finally:\n        await session.close()\n</code></pre>"},{"location":"reference/#uniconndbbridge.async_.AsyncDatabaseManager.test_connection","title":"<code>test_connection()</code>  <code>async</code>","text":"<p>Test the database connection asynchronously.</p> Source code in <code>uniconndbbridge/async_.py</code> <pre><code>async def test_connection(self) -&gt; bool:\n    \"\"\"Test the database connection asynchronously.\"\"\"\n    try:\n        async with self.connection() as conn:\n            await conn.execute(text(\"SELECT 1\"))\n        logger.info(\"Async connection test successful\")\n        return True\n    except Exception as e:\n        logger.warning(f\"Async connection test failed: {e}\")\n        return False\n</code></pre>"},{"location":"reference/#uniconndbbridge.config.DBConfig","title":"<code>uniconndbbridge.config.DBConfig</code>  <code>dataclass</code>","text":"<p>Database configuration class.</p> Source code in <code>uniconndbbridge/config.py</code> <pre><code>@dataclass\nclass DBConfig:\n    \"\"\"Database configuration class.\"\"\"\n\n    dialect: str\n    user: str\n    password: str\n    host: str = \"localhost\"\n    port: Optional[int] = None\n    database: Optional[str] = None\n    driver: Optional[str] = None\n    auth_plugin: Optional[str] = None\n    auth_options: Dict[str, Any] = field(default_factory=dict)\n    pool_size: int = 10\n    max_overflow: int = 20\n    pool_timeout: int = 30\n    pool_recycle: int = 3600\n    pool_pre_ping: bool = True\n    echo: bool = False\n    engine_options: Dict[str, Any] = field(default_factory=dict)\n\n    def __post_init__(self):\n        \"\"\"Post-initialization to set default driver if not specified.\"\"\"\n        if self.driver is None:\n            self.driver = self.get_default_driver()\n\n    def get_default_driver(self) -&gt; str:\n        \"\"\"Get the default driver for the dialect.\"\"\"\n        from .registry import DriverRegistry\n\n        dialect_info = DriverRegistry.get_dialect_info(self.dialect)\n        if not dialect_info:\n            raise ValueError(f\"Unsupported dialect: {self.dialect}\")\n\n        return dialect_info.default_driver\n\n    def set_driver(self, driver: Optional[str] = None) -&gt; None:\n        \"\"\"Set the driver, using default if None is provided.\"\"\"\n        if driver is None:\n            self.driver = self.get_default_driver()\n        else:\n            # Validate the driver\n            from .registry import DriverRegistry\n            if not DriverRegistry.validate_driver(self.dialect, driver):\n                raise ValueError(f\"Driver '{driver}' is not supported for dialect '{self.dialect}'\")\n            self.driver = driver\n\n    def to_url(self, driver_override: Optional[str] = None) -&gt; str:\n        \"\"\"Convert configuration to SQLAlchemy URL.\"\"\"\n        from .registry import DriverRegistry\n\n        # Get dialect info for default port\n        dialect_info = DriverRegistry.get_dialect_info(self.dialect)\n        if not dialect_info:\n            raise ValueError(f\"Unsupported dialect: {self.dialect}\")\n\n        # Determine driver\n        effective_driver = driver_override or self.driver or dialect_info.default_driver\n\n        # Determine port\n        effective_port = self.port or dialect_info.default_port\n\n        # URL encode credentials\n        encoded_user = quote_plus(self.user)\n        encoded_password = quote_plus(self.password)\n\n        # Build URL\n        if self.dialect == \"sqlite\":\n            if self.database:\n                return f\"sqlite:///{self.database}\"\n            else:\n                return \"sqlite:///:memory:\"\n\n        # For other databases\n        url_parts = [f\"{self.dialect}\"]\n        if effective_driver:\n            url_parts.append(f\"+{effective_driver}\")\n\n        url_parts.extend([\"://\", f\"{encoded_user}:{encoded_password}@{self.host}\"])\n\n        if effective_port:\n            url_parts.append(f\":{effective_port}\")\n\n        if self.database:\n            url_parts.append(f\"/{self.database}\")\n\n        return \"\".join(url_parts)\n\n    def get_engine_options(self) -&gt; Dict[str, Any]:\n        \"\"\"Get engine options for SQLAlchemy.\"\"\"\n        options = {\n            \"pool_size\": self.pool_size,\n            \"max_overflow\": self.max_overflow,\n            \"pool_timeout\": self.pool_timeout,\n            \"pool_recycle\": self.pool_recycle,\n            \"pool_pre_ping\": self.pool_pre_ping,\n            \"echo\": self.echo,\n        }\n\n        # Add custom engine options\n        options.update(self.engine_options)\n\n        return options\n</code></pre>"},{"location":"reference/#uniconndbbridge.config.DBConfig.__post_init__","title":"<code>__post_init__()</code>","text":"<p>Post-initialization to set default driver if not specified.</p> Source code in <code>uniconndbbridge/config.py</code> <pre><code>def __post_init__(self):\n    \"\"\"Post-initialization to set default driver if not specified.\"\"\"\n    if self.driver is None:\n        self.driver = self.get_default_driver()\n</code></pre>"},{"location":"reference/#uniconndbbridge.config.DBConfig.get_default_driver","title":"<code>get_default_driver()</code>","text":"<p>Get the default driver for the dialect.</p> Source code in <code>uniconndbbridge/config.py</code> <pre><code>def get_default_driver(self) -&gt; str:\n    \"\"\"Get the default driver for the dialect.\"\"\"\n    from .registry import DriverRegistry\n\n    dialect_info = DriverRegistry.get_dialect_info(self.dialect)\n    if not dialect_info:\n        raise ValueError(f\"Unsupported dialect: {self.dialect}\")\n\n    return dialect_info.default_driver\n</code></pre>"},{"location":"reference/#uniconndbbridge.config.DBConfig.get_engine_options","title":"<code>get_engine_options()</code>","text":"<p>Get engine options for SQLAlchemy.</p> Source code in <code>uniconndbbridge/config.py</code> <pre><code>def get_engine_options(self) -&gt; Dict[str, Any]:\n    \"\"\"Get engine options for SQLAlchemy.\"\"\"\n    options = {\n        \"pool_size\": self.pool_size,\n        \"max_overflow\": self.max_overflow,\n        \"pool_timeout\": self.pool_timeout,\n        \"pool_recycle\": self.pool_recycle,\n        \"pool_pre_ping\": self.pool_pre_ping,\n        \"echo\": self.echo,\n    }\n\n    # Add custom engine options\n    options.update(self.engine_options)\n\n    return options\n</code></pre>"},{"location":"reference/#uniconndbbridge.config.DBConfig.set_driver","title":"<code>set_driver(driver=None)</code>","text":"<p>Set the driver, using default if None is provided.</p> Source code in <code>uniconndbbridge/config.py</code> <pre><code>def set_driver(self, driver: Optional[str] = None) -&gt; None:\n    \"\"\"Set the driver, using default if None is provided.\"\"\"\n    if driver is None:\n        self.driver = self.get_default_driver()\n    else:\n        # Validate the driver\n        from .registry import DriverRegistry\n        if not DriverRegistry.validate_driver(self.dialect, driver):\n            raise ValueError(f\"Driver '{driver}' is not supported for dialect '{self.dialect}'\")\n        self.driver = driver\n</code></pre>"},{"location":"reference/#uniconndbbridge.config.DBConfig.to_url","title":"<code>to_url(driver_override=None)</code>","text":"<p>Convert configuration to SQLAlchemy URL.</p> Source code in <code>uniconndbbridge/config.py</code> <pre><code>def to_url(self, driver_override: Optional[str] = None) -&gt; str:\n    \"\"\"Convert configuration to SQLAlchemy URL.\"\"\"\n    from .registry import DriverRegistry\n\n    # Get dialect info for default port\n    dialect_info = DriverRegistry.get_dialect_info(self.dialect)\n    if not dialect_info:\n        raise ValueError(f\"Unsupported dialect: {self.dialect}\")\n\n    # Determine driver\n    effective_driver = driver_override or self.driver or dialect_info.default_driver\n\n    # Determine port\n    effective_port = self.port or dialect_info.default_port\n\n    # URL encode credentials\n    encoded_user = quote_plus(self.user)\n    encoded_password = quote_plus(self.password)\n\n    # Build URL\n    if self.dialect == \"sqlite\":\n        if self.database:\n            return f\"sqlite:///{self.database}\"\n        else:\n            return \"sqlite:///:memory:\"\n\n    # For other databases\n    url_parts = [f\"{self.dialect}\"]\n    if effective_driver:\n        url_parts.append(f\"+{effective_driver}\")\n\n    url_parts.extend([\"://\", f\"{encoded_user}:{encoded_password}@{self.host}\"])\n\n    if effective_port:\n        url_parts.append(f\":{effective_port}\")\n\n    if self.database:\n        url_parts.append(f\"/{self.database}\")\n\n    return \"\".join(url_parts)\n</code></pre>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/","title":"UniConnDBBridge \u2015 \u6700\u7d42\u4ed5\u69d8\u66f8 (2025-06-26\u7248)","text":"<p>\u3059\u3079\u3066\u306e\u5408\u610f\u5185\u5bb9\u3092 \u91cd\u8907\u306a\u304f 1 \u672c\u306b\u7d71\u5408\u3057\u305f\u6c7a\u5b9a\u7248\u3067\u3059\u3002 \u3053\u306e\u30c9\u30ad\u30e5\u30e1\u30f3\u30c8\u3060\u3051\u3067\u30ea\u30dd\u30b8\u30c8\u30ea\u3092\u7528\u610f\u3057\u3001\u958b\u767a\u30fb\u516c\u958b\u307e\u3067\u9032\u3081\u3089\u308c\u307e\u3059\u3002</p>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#1","title":"1. \u6982\u8981","text":"\u9805\u76ee \u5185\u5bb9 \u30e9\u30a4\u30d6\u30e9\u30ea\u540d UniConnDBBridge \u76ee\u7684 SQLAlchemy \u304c\u6271\u3048\u308b\u3042\u3089\u3086\u308b RDBMS\u30fb\u8a8d\u8a3c\u65b9\u5f0f\u3092\u900f\u904e\u5316\u3057\u3001\u540c\u671f\uff0f\u975e\u540c\u671f API \u3092\u4e00\u672c\u5316 \u4e3b\u306a\u7279\u5fb4 \u30c9\u30e9\u30a4\u30d0\u81ea\u52d5\u9078\u5b9a\u3001\u63a5\u7d9a\u60c5\u5831\u81ea\u52d5\u5224\u5b9a\u3001Poetry \u30d1\u30c3\u30b1\u30fc\u30b8\u3001MkDocs \u30ea\u30d5\u30a1\u30ec\u30f3\u30b9\u3001GitHub Actions CI/CD \u30e9\u30a4\u30bb\u30f3\u30b9 MIT \u30d0\u30fc\u30b8\u30e7\u30cb\u30f3\u30b0 CalVer <code>YY.MM.PATCH</code>\uff08\u4f8b <code>25.6.0</code>\uff09"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#2-fr","title":"2. \u6a5f\u80fd\u8981\u4ef6\uff08FR\uff09","text":"ID \u8981\u4ef6 FR-01 \u63a5\u7d9a\u62bd\u8c61\u5316\uff1aURL\uff0fEngine\uff0fSession \u751f\u6210\u3092 <code>DatabaseManager</code> \u306b\u7d71\u5408 FR-02 \u591a\u69d8\u306a\u8a8d\u8a3c\uff1aPW\uff0fSSL\uff0fKerberos\uff0fIAM\uff0fOAuth2 \u306a\u3069\u3092\u30d7\u30e9\u30b0\u30a4\u30f3\u5316 FR-03 \u540c\u671f (<code>DatabaseManager</code>) \u3068\u975e\u540c\u671f (<code>AsyncDatabaseManager</code>) API FR-04 \u30b3\u30cd\u30af\u30b7\u30e7\u30f3\u30d7\u30fc\u30eb\u7d30\u5206\u8a2d\u5b9a FR-05 \u30c8\u30e9\u30f3\u30b6\u30af\u30b7\u30e7\u30f3 ContextManager \u3067\u81ea\u52d5 commit/rollback FR-06 CRUD\u30fb\u751f SQL\u30fb\u30e1\u30bf\u30c7\u30fc\u30bf\u53d6\u5f97\u30e6\u30fc\u30c6\u30a3\u30ea\u30c6\u30a3 FR-07 ORM \u9023\u643a\uff08Declarative Base \u5171\u6709\uff09 FR-08 DB \u4f9d\u5b58\u4f8b\u5916\u3092\u72ec\u81ea\u968e\u5c64\u3078\u6b63\u898f\u5316 FR-09 \u6a19\u6e96 <code>logging</code> + OpenTelemetry \u30c8\u30ec\u30fc\u30b7\u30f3\u30b0 FR-10 Alembic \u7528\u8a2d\u5b9a\u30c6\u30f3\u30d7\u30ec\u30fc\u30c8\u751f\u6210 FR-11 <code>connect_info()</code>\uff1a\u63a5\u7d9a\u6e08\u307f Engine \u304b\u3089 dialect/driver \u7b49\u3092\u53d6\u5f97 FR-12 \u4e0a\u8a18\u60c5\u5831\u3092\u30ed\u30b0\u3084\u30a8\u30e9\u30fc\u8868\u793a\u306b\u518d\u5229\u7528 FR-13 <code>AsyncDatabaseManager.connect_info_async()</code> \u3082\u63d0\u4f9b FR-14 \u63a2\u7d22\u7684 URL \u751f\u6210 <code>discover_connection()</code>\uff0f<code>discover_connection_async()</code>\u2003\u2015 \u57fa\u672c\u60c5\u5831\uff08user/password/host/port/database\uff09\u3060\u3051\u3067\u63a5\u7d9a\u30c6\u30b9\u30c8\u3092\u9806\u6b21\u5b9f\u65bd\u3057\u3001\u6210\u529f\u3057\u305f URL \u3092\u8fd4\u3059 FR-15 <code>DatabaseManager.from_discovery()</code> \u3068\u975e\u540c\u671f\u7248\u3067\u7c21\u6613\u5229\u7528 FR-16 \u63a2\u7d22\u5bfe\u8c61 dialect\u30fb\u30bf\u30a4\u30e0\u30a2\u30a6\u30c8\u3092\u30e6\u30fc\u30b6\u30fc\u6307\u5b9a\u53ef\u80fd FR-17 \u4e0a\u8a18\u6a5f\u80fd\u3092\u30c9\u30ad\u30e5\u30e1\u30f3\u30c8\u3068\u30c6\u30b9\u30c8\u3067\u30ab\u30d0\u30ec\u30c3\u30b8\u78ba\u4fdd"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#3-nfr","title":"3. \u975e\u6a5f\u80fd\u8981\u4ef6\uff08NFR\uff09","text":"ID \u8981\u4ef6 NFR-01 Poetry \u3067\u4f9d\u5b58\u3068\u30d3\u30eb\u30c9\u7ba1\u7406 NFR-02 \u5fc5\u9808\u4f9d\u5b58\u306f\u6700\u5c0f (<code>SQLAlchemy 2.x</code>, <code>typing-extensions</code>)\u3001\u4ed6\u306f extras \u3067\u5206\u96e2 NFR-03 \u30b9\u30ec\u30c3\u30c9\u30bb\u30fc\u30d5\uff08\u540c\u671f API\uff09 NFR-04 \u578b\u30d2\u30f3\u30c8 100 %\u30fbMyPy strict 0 \u30a8\u30e9\u30fc NFR-05 MkDocs-Material + mkdocstrings \u3067 API \u30ea\u30d5\u30a1\u30ec\u30f3\u30b9\u81ea\u52d5\u751f\u6210\u30fbGitHub Pages \u516c\u958b NFR-06 pytest(+asyncio) \u3067 90 %\u4ee5\u4e0a\u30ab\u30d0\u30ec\u30c3\u30b8 NFR-07 GitHub Actions\uff1alint\u2192test\u2192build\u2192PyPI\uff0fdocs\u2192Pages NFR-08 CalVer \u306b\u3088\u308b\u4e88\u6e2c\u53ef\u80fd\u306a\u30ea\u30ea\u30fc\u30b9"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#4-db","title":"4. \u63a1\u7528 DB \u30c9\u30e9\u30a4\u30d0\u3068\u6839\u62e0","text":"DB \u540c\u671f\u30c9\u30e9\u30a4\u30d0 \u975e\u540c\u671f\u30c9\u30e9\u30a4\u30d0 \u63a1\u7528\u7406\u7531\uff08\u629c\u7c8b\uff09 PostgreSQL <code>psycopg2-binary</code> <code>asyncpg</code> \u516c\u5f0f\u65e2\u5b9a\u30fb\u9ad8\u901f\u30fbwheel \u63d0\u4f9b MySQL / MariaDB <code>mysqlclient</code> <code>aiomysql</code> C \u5b9f\u88c5\u9ad8\u901f\u30fb\u63a1\u7528\u4f8b\u591a\u6570 SQLite \u6a19\u6e96 <code>sqlite3</code> <code>aiosqlite</code> CPython \u540c\u68b1\u30fbAPI \u4e92\u63db Oracle <code>python-oracledb</code> \u540c\u5de6\uff08async \u5bfe\u5fdc\uff09 Oracle \u516c\u5f0f\u5f8c\u7d99 SQL Server <code>pyodbc</code> <code>aioodbc</code> (\u4efb\u610f) \u516c\u5f0f\u65b9\u8a00\u524d\u63d0\u30fb\u5168 OS wheel"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#5-poetry-pyprojecttoml","title":"5. Poetry <code>pyproject.toml</code>\uff08\u4e3b\u8981\u629c\u7c8b\uff09","text":"<pre><code>[tool.poetry]\nname = \"uniconndbbridge\"\nversion = \"25.6.0\"\ndescription = \"Universal auth-ready SQLAlchemy Database Manager\"\nlicense = \"MIT\"\npackages = [{include = \"uniconndbbridge\"}]\n\n[tool.poetry.dependencies]\npython = \"^3.9\"\nSQLAlchemy = \"^2.0\"\ntyping-extensions = \"^4.0\"\n\n[tool.poetry.extras]\npostgres          = [\"psycopg2-binary&gt;=2.9\"]\npostgres_async    = [\"asyncpg&gt;=0.29\"]\nmysql             = [\"mysqlclient&gt;=2.2\"]\nmysql_async       = [\"aiomysql&gt;=0.2\"]\nsqlite_async      = [\"aiosqlite&gt;=0.20\"]\noracle            = [\"python-oracledb&gt;=2.0\"]\nmssql             = [\"pyodbc&gt;=5.0\"]\nmssql_async       = [\"aioodbc&gt;=0.4\"]\nkerberos          = [\"pykerberos&gt;=1.2\"]\naws               = [\"boto3&gt;=1.34\", \"botocore&gt;=1.34\"]\ngcp               = [\"google-auth&gt;=2.0\"]\nazure             = [\"azure-identity&gt;=1.15\"]\ndocs              = [\"mkdocs-material&gt;=9.5\", \"mkdocstrings[python]&gt;=0.25\",\n                     \"pymdown-extensions&gt;=10.7\",\n                     \"mkdocs-git-revision-date-localized-plugin&gt;=1.2\"]\n</code></pre>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#6","title":"6. \u30c7\u30a3\u30ec\u30af\u30c8\u30ea\u69cb\u6210","text":"<pre><code>uniconndbbridge/\n\u251c\u2500\u2500 uniconndbbridge/          # \u30e9\u30a4\u30d6\u30e9\u30ea\u672c\u4f53\n\u2502   \u251c\u2500\u2500 __init__.py\n\u2502   \u251c\u2500\u2500 core.py              # DatabaseManager\n\u2502   \u251c\u2500\u2500 async_.py            # AsyncDatabaseManager\n\u2502   \u251c\u2500\u2500 discovery.py         # discover_connection \u5b9f\u88c5\n\u2502   \u251c\u2500\u2500 config.py            # DBConfig &amp; Auth plugins\n\u2502   \u251c\u2500\u2500 exceptions.py\n\u2502   \u251c\u2500\u2500 registry.py\n\u2502   \u2514\u2500\u2500 py.typed\n\u251c\u2500\u2500 docs/                    # MkDocs \u30bd\u30fc\u30b9\n\u251c\u2500\u2500 tests/\n\u2514\u2500\u2500 .github/workflows/       # ci.yml / docs.yml\n</code></pre>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#7-api","title":"7. \u4e3b\u8981 API \u30b7\u30b0\u30cd\u30c1\u30e3","text":"<pre><code>class DatabaseManager:\n    def __init__(self, url: str | None = None, *, config: DBConfig | dict | None = None, ...)\n\n    # \u81ea\u52d5\u5224\u5b9a\n    def connect_info(self) -&gt; ConnectionInfo: ...\n\n    # \u63a2\u7d22\u7684 URL \u751f\u6210\n    @classmethod\n    def discover_connection(cls, *, user: str, password: str, host: str = \"localhost\",\n                            port: int | None = None, database: str | None = None,\n                            dialects: Sequence[str] = DEFAULT_DIALECTS,\n                            timeout: float = 5.0) -&gt; str: ...\n\n    # \u7c21\u6613\u69cb\u7bc9\n    @classmethod\n    def from_discovery(cls, **kwargs) -&gt; \"DatabaseManager\": ...\n</code></pre> <p>\u975e\u540c\u671f\u7248 (<code>AsyncDatabaseManager</code>) \u306f\u540c\u7b49\u306e <code>async</code>/<code>await</code> \u30e1\u30bd\u30c3\u30c9\u3092\u5099\u3048\u307e\u3059\u3002</p>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#8","title":"8. \u30c9\u30ad\u30e5\u30e1\u30f3\u30c8 &amp; \u30b5\u30a4\u30c8\u516c\u958b","text":"<ul> <li><code>mkdocs.yml</code> \u3067\u30b5\u30a4\u30c8\u30e1\u30bf\u30fb\u30ca\u30d3\u30b2\u30fc\u30b7\u30e7\u30f3\u5b9a\u7fa9</li> <li>mkdocstrings \u3067 Docstring \u304b\u3089 API \u81ea\u52d5\u751f\u6210</li> <li>GitHub Actions <code>docs.yml</code> \u304c <code>main</code> push \u6642\u306b\u30d3\u30eb\u30c9\u2192<code>gh-pages</code> \u3078\u30c7\u30d7\u30ed\u30a4</li> <li>GitHub Settings \u2192 Pages \u3067 Source: GitHub Actions \u6307\u5b9a</li> </ul>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#9-cicd","title":"9. CI/CD \u6982\u8981","text":""},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#ciyml","title":"ci.yml","text":"<ol> <li>OS\u00d7Python \u30de\u30c8\u30ea\u30af\u30b9\u3067 ruff \u2192 mypy \u2192 pytest(cov) \u2192 wheel</li> <li>\u30bf\u30b0 push \u6642\u306b Poetry publish (PyPI)</li> </ol>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#docsyml","title":"docs.yml","text":"<ol> <li>Poetry install (<code>docs</code> extra) \u2192 <code>mkdocs build --strict</code></li> <li><code>actions/upload-pages-artifact</code> \u2192 <code>actions/deploy-pages</code></li> </ol>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#10","title":"10. \u958b\u767a\u30ef\u30fc\u30af\u30d5\u30ed\u30fc","text":"<pre><code># \u30bb\u30c3\u30c8\u30a2\u30c3\u30d7\npoetry install --with dev,docs\n\n# Lint / \u578b\u30c1\u30a7\u30c3\u30af / \u30c6\u30b9\u30c8\npoetry run ruff .\npoetry run mypy uniconndbbridge\npoetry run pytest -q --cov=uniconndbbridge\n\n# \u30c9\u30ad\u30e5\u30e1\u30f3\u30c8\u7de8\u96c6\npoetry run mkdocs serve\n\n# \u30ea\u30ea\u30fc\u30b9\npoetry version 25.6.1\ngit tag -s 25.6.1 -m \"Release 25.6.1\"\ngit push &amp;&amp; git push --tags\n</code></pre>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#11","title":"11. \u62e1\u5f35\u30ed\u30fc\u30c9\u30de\u30c3\u30d7\uff08\u629c\u7c8b\uff09","text":"<ol> <li>\u30de\u30eb\u30c1\u30ec\u30d7\u30ea\u30ab\u81ea\u52d5\u30eb\u30fc\u30c6\u30a3\u30f3\u30b0 (<code>READ/WRITE</code> \u5207\u66ff)</li> <li>Prometheus \u30e1\u30c8\u30ea\u30af\u30b9\u30a8\u30af\u30b9\u30dd\u30fc\u30bf</li> <li>NoSQL Dialect (MongoSQL \u7b49) \u30d6\u30ea\u30c3\u30b8</li> <li>CLI \u30c4\u30fc\u30eb <code>ucdb</code> \u3067\u306e\u5bfe\u8a71\u7684\u30a2\u30af\u30bb\u30b9</li> </ol>"},{"location":"design/%E5%9F%BA%E6%9C%AC%E8%A8%AD%E8%A8%88%E6%9B%B8/#_1","title":"\ud83c\udf89 \u7d50\u8ad6","text":"<p>UniConnDBBridge \u306f</p> <ul> <li>\u5168\u4e3b\u8981 RDBMS \uff0b \u591a\u69d8\u306a\u8a8d\u8a3c \u3092\u300cextras &amp; \u30d7\u30e9\u30b0\u30a4\u30f3\u300d\u3067\u67d4\u8edf\u30b5\u30dd\u30fc\u30c8</li> <li>URL \u81ea\u52d5\u63a2\u7d22 \u3068 \u63a5\u7d9a\u60c5\u5831\u53ef\u8996\u5316 \u3067\u30e6\u30fc\u30b6\u30fc\u64cd\u4f5c\u3092\u6700\u5c0f\u5316</li> <li>Poetry\u30fbMkDocs\u30fbGitHub Actions \u306b\u3088\u308b\u30e2\u30c0\u30f3\u306a\u30e9\u30a4\u30d6\u30e9\u30ea\u904b\u7528\u3092\u6a19\u6e96\u88c5\u5099</li> </ul> <p>\u3053\u306e\u6700\u7d42\u4ed5\u69d8\u3092\u57fa\u306b\u3001\u3059\u3050\u306b\u3067\u3082\u5b9f\u88c5\u3068\u516c\u958b\u3092\u59cb\u3081\u3089\u308c\u307e\u3059\u3002 \u8ffd\u52a0\u306e\u8cea\u554f\u3084\u30ec\u30d3\u30e5\u30fc\u4f9d\u983c\u304c\u3042\u308c\u3070\u3001\u304a\u6c17\u8efd\u306b\u3069\u3046\u305e\uff01</p>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/","title":"\u8a73\u7d30\u8a2d\u8a08\u66f8","text":"<pre><code>        # \u30d1\u30b9\u30ef\u30fc\u30c9\u3092\u30c8\u30fc\u30af\u30f3\u306b\u7f6e\u63db\n        new_netloc = f\"{parsed.username}:{token}@{parsed.hostname}\"\n        if parsed.port:\n            new_netloc += f\":{parsed.port}\"\n\n        # \u65b0\u3057\u3044 URL \u3092\u69cb\u7bc9\n        new_parsed = parsed._replace(netloc=new_netloc)\n        return urlunparse(new_parsed)\n\n    def configure_engine_args(self) -&gt; Dict[str, Any]:\n        # \u30d7\u30fc\u30eb\u8a2d\u5b9a\u3092\u8abf\u6574\uff08\u30c8\u30fc\u30af\u30f3\u306e\u6709\u52b9\u671f\u9650\u5bfe\u5fdc\uff09\n        return {\n            \"pool_recycle\": 900,  # 15\u5206\u3067\u30ea\u30b5\u30a4\u30af\u30eb\n            \"pool_pre_ping\": True\n        }\n\n    async def configure_engine_args_async(self) -&gt; Dict[str, Any]:\n        # \u975e\u540c\u671f\u7248\u3082\u540c\u3058\u8a2d\u5b9a\n        return self.configure_engine_args()\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#5","title":"5. \u30e6\u30fc\u30c6\u30a3\u30ea\u30c6\u30a3\u8a73\u7d30\u8a2d\u8a08","text":""},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#51-crud","title":"5.1 CRUD \u30d8\u30eb\u30d1\u30fc","text":"<pre><code># utils/crud.py\nfrom typing import Type, List, Dict, Any, Optional\nfrom sqlalchemy import select, insert, update, delete\nfrom sqlalchemy.orm import DeclarativeBase\n\nclass CRUDMixin:\n    \"\"\"CRUD \u64cd\u4f5c\u30df\u30c3\u30af\u30b9\u30a4\u30f3\"\"\"\n\n    def __init__(self, manager: Union[DatabaseManager, AsyncDatabaseManager]):\n        self.manager = manager\n\n    # \u540c\u671f\u7248 CRUD\n    def create(\n        self,\n        model: Type[DeclarativeBase],\n        **values: Any\n    ) -&gt; DeclarativeBase:\n        \"\"\"\u30ec\u30b3\u30fc\u30c9\u4f5c\u6210\"\"\"\n        with self.manager.session() as session:\n            instance = model(**values)\n            session.add(instance)\n            session.flush()\n            session.refresh(instance)\n            return instance\n\n    def read(\n        self,\n        model: Type[DeclarativeBase],\n        **filters: Any\n    ) -&gt; Optional[DeclarativeBase]:\n        \"\"\"\u5358\u4e00\u30ec\u30b3\u30fc\u30c9\u8aad\u307f\u53d6\u308a\"\"\"\n        with self.manager.session() as session:\n            stmt = select(model).filter_by(**filters)\n            return session.execute(stmt).scalar_one_or_none()\n\n    def read_many(\n        self,\n        model: Type[DeclarativeBase],\n        limit: Optional[int] = None,\n        offset: Optional[int] = None,\n        **filters: Any\n    ) -&gt; List[DeclarativeBase]:\n        \"\"\"\u8907\u6570\u30ec\u30b3\u30fc\u30c9\u8aad\u307f\u53d6\u308a\"\"\"\n        with self.manager.session() as session:\n            stmt = select(model).filter_by(**filters)\n            if limit:\n                stmt = stmt.limit(limit)\n            if offset:\n                stmt = stmt.offset(offset)\n            return list(session.execute(stmt).scalars())\n\n    def update(\n        self,\n        model: Type[DeclarativeBase],\n        values: Dict[str, Any],\n        **filters: Any\n    ) -&gt; int:\n        \"\"\"\u30ec\u30b3\u30fc\u30c9\u66f4\u65b0\"\"\"\n        with self.manager.session() as session:\n            stmt = update(model).filter_by(**filters).values(**values)\n            result = session.execute(stmt)\n            return result.rowcount\n\n    def delete(\n        self,\n        model: Type[DeclarativeBase],\n        **filters: Any\n    ) -&gt; int:\n        \"\"\"\u30ec\u30b3\u30fc\u30c9\u524a\u9664\"\"\"\n        with self.manager.session() as session:\n            stmt = delete(model).filter_by(**filters)\n            result = session.execute(stmt)\n            return result.rowcount\n\n\nclass AsyncCRUDMixin:\n    \"\"\"\u975e\u540c\u671f CRUD \u64cd\u4f5c\u30df\u30c3\u30af\u30b9\u30a4\u30f3\"\"\"\n\n    def __init__(self, manager: AsyncDatabaseManager):\n        self.manager = manager\n\n    async def create(\n        self,\n        model: Type[DeclarativeBase],\n        **values: Any\n    ) -&gt; DeclarativeBase:\n        \"\"\"\u975e\u540c\u671f\u30ec\u30b3\u30fc\u30c9\u4f5c\u6210\"\"\"\n        async with self.manager.session() as session:\n            instance = model(**values)\n            session.add(instance)\n            await session.flush()\n            await session.refresh(instance)\n            return instance\n\n    # ... (\u4ed6\u306e\u975e\u540c\u671f CRUD \u30e1\u30bd\u30c3\u30c9)\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#52","title":"5.2 \u30de\u30a4\u30b0\u30ec\u30fc\u30b7\u30e7\u30f3\u30d8\u30eb\u30d1\u30fc","text":"<pre><code># utils/migration.py\nfrom pathlib import Path\nfrom typing import Optional\nimport textwrap\n\nclass AlembicHelper:\n    \"\"\"Alembic \u8a2d\u5b9a\u30d8\u30eb\u30d1\u30fc\"\"\"\n\n    @staticmethod\n    def generate_alembic_ini(\n        manager: DatabaseManager,\n        script_location: str = \"migrations\",\n        compare_type: bool = True,\n        compare_server_default: bool = True\n    ) -&gt; str:\n        \"\"\"alembic.ini \u30c6\u30f3\u30d7\u30ec\u30fc\u30c8\u751f\u6210\"\"\"\n\n        template = textwrap.dedent(f\"\"\"\n        [alembic]\n        script_location = {script_location}\n        prepend_sys_path = .\n        version_path_separator = os\n        sqlalchemy.url = {manager._url}\n\n        [post_write_hooks]\n        hooks = black\n        black.type = console_scripts\n        black.entrypoint = black\n        black.options = -l 88\n\n        [loggers]\n        keys = root,sqlalchemy,alembic\n\n        [handlers]\n        keys = console\n\n        [formatters]\n        keys = generic\n\n        [logger_root]\n        level = WARN\n        handlers = console\n        qualname =\n\n        [logger_sqlalchemy]\n        level = WARN\n        handlers =\n        qualname = sqlalchemy.engine\n\n        [logger_alembic]\n        level = INFO\n        handlers =\n        qualname = alembic\n\n        [handler_console]\n        class = StreamHandler\n        args = (sys.stderr,)\n        level = NOTSET\n        formatter = generic\n\n        [formatter_generic]\n        format = %(levelname)-5.5s [%(name)s] %(message)s\n        datefmt = %H:%M:%S\n        \"\"\")\n\n        return template.strip()\n\n    @staticmethod\n    def generate_env_py(\n        target_metadata: Optional[MetaData] = None,\n        compare_type: bool = True,\n        compare_server_default: bool = True\n    ) -&gt; str:\n        \"\"\"env.py \u30c6\u30f3\u30d7\u30ec\u30fc\u30c8\u751f\u6210\"\"\"\n\n        template = textwrap.dedent(\"\"\"\n        from logging.config import fileConfig\n        from sqlalchemy import engine_from_config\n        from sqlalchemy import pool\n        from alembic import context\n\n        # this is the Alembic Config object\n        config = context.config\n\n        # Interpret the config file for Python logging\n        if config.config_file_name is not None:\n            fileConfig(config.config_file_name)\n\n        # add your model's MetaData object here\n        target_metadata = None  # TODO: Import your metadata\n\n        def run_migrations_offline() -&gt; None:\n            '''Run migrations in 'offline' mode.'''\n            url = config.get_main_option(\"sqlalchemy.url\")\n            context.configure(\n                url=url,\n                target_metadata=target_metadata,\n                literal_binds=True,\n                dialect_opts={\"paramstyle\": \"named\"},\n                compare_type={compare_type},\n                compare_server_default={compare_server_default}\n            )\n\n            with context.begin_transaction():\n                context.run_migrations()\n\n        def run_migrations_online() -&gt; None:\n            '''Run migrations in 'online' mode.'''\n            connectable = engine_from_config(\n                config.get_section(config.config_ini_section),\n                prefix=\"sqlalchemy.\",\n                poolclass=pool.NullPool,\n            )\n\n            with connectable.connect() as connection:\n                context.configure(\n                    connection=connection,\n                    target_metadata=target_metadata,\n                    compare_type={compare_type},\n                    compare_server_default={compare_server_default}\n                )\n\n                with context.begin_transaction():\n                    context.run_migrations()\n\n        if context.is_offline_mode():\n            run_migrations_offline()\n        else:\n            run_migrations_online()\n        \"\"\").format(\n            compare_type=compare_type,\n            compare_server_default=compare_server_default\n        )\n\n        return template.strip()\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#6","title":"6. \u30ed\u30ae\u30f3\u30b0\u30fb\u30e2\u30cb\u30bf\u30ea\u30f3\u30b0\u8a73\u7d30\u8a2d\u8a08","text":""},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#61","title":"6.1 \u30ed\u30ae\u30f3\u30b0\u8a2d\u5b9a","text":"<pre><code># logging_config.py\nimport logging\nimport logging.config\nfrom typing import Dict, Any\n\ndef setup_logging(\n    level: str = \"INFO\",\n    format: str = \"%(asctime)s - %(name)s - %(levelname)s - %(message)s\",\n    enable_sql_echo: bool = False\n) -&gt; None:\n    \"\"\"\u30ed\u30ae\u30f3\u30b0\u8a2d\u5b9a\"\"\"\n\n    config: Dict[str, Any] = {\n        \"version\": 1,\n        \"disable_existing_loggers\": False,\n        \"formatters\": {\n            \"default\": {\n                \"format\": format,\n                \"datefmt\": \"%Y-%m-%d %H:%M:%S\"\n            },\n            \"detailed\": {\n                \"format\": \"%(asctime)s - %(name)s - %(levelname)s - %(filename)s:%(lineno)d - %(message)s\",\n                \"datefmt\": \"%Y-%m-%d %H:%M:%S\"\n            }\n        },\n        \"handlers\": {\n            \"console\": {\n                \"class\": \"logging.StreamHandler\",\n                \"level\": level,\n                \"formatter\": \"default\",\n                \"stream\": \"ext://sys.stdout\"\n            },\n            \"file\": {\n                \"class\": \"logging.handlers.RotatingFileHandler\",\n                \"level\": \"DEBUG\",\n                \"formatter\": \"detailed\",\n                \"filename\": \"uniconndbbridge.log\",\n                \"maxBytes\": 10485760,  # 10MB\n                \"backupCount\": 5\n            }\n        },\n        \"loggers\": {\n            \"uniconndbbridge\": {\n                \"level\": level,\n                \"handlers\": [\"console\", \"file\"],\n                \"propagate\": False\n            },\n            \"sqlalchemy.engine\": {\n                \"level\": \"INFO\" if enable_sql_echo else \"WARNING\",\n                \"handlers\": [\"console\"],\n                \"propagate\": False\n            },\n            \"sqlalchemy.pool\": {\n                \"level\": \"DEBUG\" if enable_sql_echo else \"WARNING\",\n                \"handlers\": [\"console\"],\n                \"propagate\": False\n            }\n        },\n        \"root\": {\n            \"level\": \"WARNING\",\n            \"handlers\": [\"console\"]\n        }\n    }\n\n    logging.config.dictConfig(config)\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#62-opentelemetry","title":"6.2 OpenTelemetry \u7d71\u5408","text":"<pre><code># telemetry.py\nfrom typing import Optional, Dict, Any\nfrom contextlib import contextmanager\nfrom opentelemetry import trace\nfrom opentelemetry.trace import Tracer, Span\nfrom opentelemetry.instrumentation.sqlalchemy import SQLAlchemyInstrumentor\n\nclass TelemetryManager:\n    \"\"\"\u30c6\u30ec\u30e1\u30c8\u30ea\u7ba1\u7406\"\"\"\n\n    def __init__(self, service_name: str = \"uniconndbbridge\"):\n        self.service_name = service_name\n        self.tracer: Optional[Tracer] = None\n        self._instrumentor: Optional[SQLAlchemyInstrumentor] = None\n\n    def initialize(\n        self,\n        endpoint: Optional[str] = None,\n        headers: Optional[Dict[str, str]] = None\n    ) -&gt; None:\n        \"\"\"\u30c6\u30ec\u30e1\u30c8\u30ea\u521d\u671f\u5316\"\"\"\n        from opentelemetry.exporter.otlp.proto.grpc.trace_exporter import OTLPSpanExporter\n        from opentelemetry.sdk.trace import TracerProvider\n        from opentelemetry.sdk.trace.export import BatchSpanProcessor\n        from opentelemetry.sdk.resources import Resource\n\n        # \u30ea\u30bd\u30fc\u30b9\u8a2d\u5b9a\n        resource = Resource.create({\n            \"service.name\": self.service_name,\n            \"service.version\": \"25.6.0\"\n        })\n\n        # \u30d7\u30ed\u30d0\u30a4\u30c0\u30fc\u8a2d\u5b9a\n        provider = TracerProvider(resource=resource)\n\n        # \u30a8\u30af\u30b9\u30dd\u30fc\u30bf\u30fc\u8a2d\u5b9a\n        if endpoint:\n            exporter = OTLPSpanExporter(\n                endpoint=endpoint,\n                headers=headers\n            )\n            processor = BatchSpanProcessor(exporter)\n            provider.add_span_processor(processor)\n\n        # \u30b0\u30ed\u30fc\u30d0\u30eb\u8a2d\u5b9a\n        trace.set_tracer_provider(provider)\n        self.tracer = trace.get_tracer(__name__)\n\n        # SQLAlchemy \u30a4\u30f3\u30b9\u30c8\u30eb\u30e1\u30f3\u30c6\u30fc\u30b7\u30e7\u30f3\n        self._instrumentor = SQLAlchemyInstrumentor()\n\n    def instrument_engine(self, engine: Engine) -&gt; None:\n        \"\"\"Engine \u306b\u30c6\u30ec\u30e1\u30c8\u30ea\u3092\u9069\u7528\"\"\"\n        if self._instrumentor:\n            self._instrumentor.instrument(\n                engine=engine,\n                service=self.service_name\n            )\n\n    @contextmanager\n    def span(self, name: str, **attributes: Any):\n        \"\"\"\u30b9\u30d1\u30f3\u30b3\u30f3\u30c6\u30ad\u30b9\u30c8\u30de\u30cd\u30fc\u30b8\u30e3\u30fc\"\"\"\n        if not self.tracer:\n            yield None\n            return\n\n        with self.tracer.start_as_current_span(name) as span:\n            for key, value in attributes.items():\n                span.set_attribute(key, str(value))\n            yield span\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#7","title":"7. \u30c6\u30b9\u30c8\u6226\u7565\u8a73\u7d30","text":""},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#71","title":"7.1 \u30e6\u30cb\u30c3\u30c8\u30c6\u30b9\u30c8\u69cb\u9020","text":"<pre><code># tests/test_core.py\nimport pytest\nfrom unittest.mock import Mock, patch, MagicMock\nfrom sqlalchemy import create_engine\nfrom uniconndbbridge import DatabaseManager, DBConfig\n\nclass TestDatabaseManager:\n    \"\"\"DatabaseManager \u30e6\u30cb\u30c3\u30c8\u30c6\u30b9\u30c8\"\"\"\n\n    @pytest.fixture\n    def mock_engine(self):\n        \"\"\"\u30e2\u30c3\u30af Engine\"\"\"\n        engine = MagicMock()\n        engine.url.database = \"testdb\"\n        engine.url.host = \"localhost\"\n        engine.url.port = 5432\n        engine.url.username = \"testuser\"\n        engine.dialect.name = \"postgresql\"\n        engine.dialect.driver = \"psycopg2\"\n        return engine\n\n    @pytest.fixture\n    def db_manager(self, mock_engine):\n        \"\"\"\u30c6\u30b9\u30c8\u7528 DatabaseManager\"\"\"\n        with patch(\"uniconndbbridge.core.create_engine\", return_value=mock_engine):\n            return DatabaseManager(url=\"postgresql://testuser:pass@localhost/testdb\")\n\n    def test_initialization_with_url(self):\n        \"\"\"URL \u3067\u306e\u521d\u671f\u5316\u30c6\u30b9\u30c8\"\"\"\n        url = \"postgresql://user:pass@localhost/db\"\n        with patch(\"uniconndbbridge.core.create_engine\") as mock_create:\n            manager = DatabaseManager(url=url)\n            assert manager._url == url\n            assert manager._config is None\n\n    def test_initialization_with_config(self):\n        \"\"\"\u8a2d\u5b9a\u3067\u306e\u521d\u671f\u5316\u30c6\u30b9\u30c8\"\"\"\n        config = DBConfig(\n            dialect=\"postgresql\",\n            user=\"testuser\",\n            password=\"testpass\",\n            host=\"localhost\",\n            database=\"testdb\"\n        )\n\n        with patch(\"uniconndbbridge.core.create_engine\"):\n            manager = DatabaseManager(config=config)\n            assert manager._config == config\n            assert \"postgresql\" in manager._url\n\n    def test_connection_info(self, db_manager, mock_engine):\n        \"\"\"\u63a5\u7d9a\u60c5\u5831\u53d6\u5f97\u30c6\u30b9\u30c8\"\"\"\n        with patch.object(db_manager, \"engine\", mock_engine):\n            info = db_manager.connect_info()\n\n            assert info.dialect == \"postgresql\"\n            assert info.driver == \"psycopg2\"\n            assert info.database == \"testdb\"\n            assert info.host == \"localhost\"\n            assert info.port == 5432\n            assert info.user == \"testuser\"\n\n    @pytest.mark.parametrize(\"dialect,expected_port\", [\n        (\"postgresql\", 5432),\n        (\"mysql\", 3306),\n        (\"oracle\", 1521),\n        (\"mssql\", 1433)\n    ])\n    def test_default_ports(self, dialect, expected_port):\n        \"\"\"\u30c7\u30d5\u30a9\u30eb\u30c8\u30dd\u30fc\u30c8\u30c6\u30b9\u30c8\"\"\"\n        from uniconndbbridge.registry import DriverRegistry\n\n        info = DriverRegistry.get_dialect_info(dialect)\n        assert info is not None\n        assert info.default_port == expected_port\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#72","title":"7.2 \u7d71\u5408\u30c6\u30b9\u30c8","text":"<pre><code># tests/integration/test_real_databases.py\nimport pytest\nimport os\nfrom uniconndbbridge import DatabaseManager, AsyncDatabaseManager\nfrom sqlalchemy import text\n\n# \u74b0\u5883\u5909\u6570\u304b\u3089\u63a5\u7d9a\u60c5\u5831\u53d6\u5f97\nPOSTGRES_URL = os.getenv(\"TEST_POSTGRES_URL\")\nMYSQL_URL = os.getenv(\"TEST_MYSQL_URL\")\n\n@pytest.mark.integration\n@pytest.mark.skipif(not POSTGRES_URL, reason=\"PostgreSQL not configured\")\nclass TestPostgreSQLIntegration:\n    \"\"\"PostgreSQL \u7d71\u5408\u30c6\u30b9\u30c8\"\"\"\n\n    def test_sync_connection(self):\n        \"\"\"\u540c\u671f\u63a5\u7d9a\u30c6\u30b9\u30c8\"\"\"\n        with DatabaseManager(url=POSTGRES_URL) as manager:\n            result = manager.execute(text(\"SELECT version()\"))\n            version = result.scalar()\n            assert \"PostgreSQL\" in version\n\n    @pytest.mark.asyncio\n    async def test_async_connection(self):\n        \"\"\"\u975e\u540c\u671f\u63a5\u7d9a\u30c6\u30b9\u30c8\"\"\"\n        async with AsyncDatabaseManager(url=POSTGRES_URL) as manager:\n            result = await manager.execute(text(\"SELECT version()\"))\n            version = result.scalar()\n            assert \"PostgreSQL\" in version\n\n    def test_discovery(self):\n        \"\"\"\u63a5\u7d9a\u63a2\u7d22\u30c6\u30b9\u30c8\"\"\"\n        # URL \u304b\u3089\u63a5\u7d9a\u60c5\u5831\u3092\u62bd\u51fa\n        from urllib.parse import urlparse\n        parsed = urlparse(POSTGRES_URL)\n\n        discovered_url = DatabaseManager.discover_connection(\n            user=parsed.username,\n            password=parsed.password,\n            host=parsed.hostname,\n            port=parsed.port,\n            database=parsed.path.lstrip(\"/\"),\n            dialects=[\"postgresql\"]\n        )\n\n        assert \"postgresql\" in discovered_url\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#73","title":"7.3 \u30e2\u30c3\u30af\u30c6\u30b9\u30c8","text":"<pre><code># tests/test_discovery.py\nimport pytest\nfrom unittest.mock import patch, MagicMock\nfrom uniconndbbridge.discovery import ConnectionDiscovery\nfrom uniconndbbridge.exceptions import DiscoveryError\n\nclass TestConnectionDiscovery:\n    \"\"\"\u63a5\u7d9a\u63a2\u7d22\u30c6\u30b9\u30c8\"\"\"\n\n    @patch(\"uniconndbbridge.discovery.create_engine\")\n    def test_successful_discovery(self, mock_create_engine):\n        \"\"\"\u6210\u529f\u3059\u308b\u63a2\u7d22\u30c6\u30b9\u30c8\"\"\"\n        # \u6700\u521d\u306e\u8a66\u884c\u3067\u6210\u529f\n        mock_engine = MagicMock()\n        mock_create_engine.return_value = mock_engine\n\n        # connect() \u304c\u6210\u529f\n        mock_conn = MagicMock()\n        mock_engine.connect.return_value.__enter__.return_value = mock_conn\n\n        url = ConnectionDiscovery.discover_connection(\n            user=\"testuser\",\n            password=\"testpass\",\n            host=\"localhost\",\n            dialects=[\"postgresql\", \"mysql\"]\n        )\n\n        assert \"postgresql\" in url\n        assert \"testuser:testpass@localhost\" in url\n\n    @patch(\"uniconndbbridge.discovery.create_engine\")\n    def test_all_dialects_fail(self, mock_create_engine):\n        \"\"\"\u5168\u65b9\u8a00\u304c\u5931\u6557\u3059\u308b\u30c6\u30b9\u30c8\"\"\"\n        # \u3059\u3079\u3066\u306e\u63a5\u7d9a\u8a66\u884c\u304c\u5931\u6557\n        mock_create_engine.side_effect = Exception(\"Connection failed\")\n\n        with pytest.raises(DiscoveryError) as exc_info:\n            ConnectionDiscovery.discover_connection(\n                user=\"testuser\",\n                password=\"testpass\",\n                host=\"localhost\",\n                dialects=[\"postgresql\", \"mysql\"]\n            )\n\n        assert \"No valid connection found\" in str(exc_info.value)\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#8","title":"8. \u30d1\u30d5\u30a9\u30fc\u30de\u30f3\u30b9\u6700\u9069\u5316","text":""},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#81","title":"8.1 \u63a5\u7d9a\u30d7\u30fc\u30ea\u30f3\u30b0\u6700\u9069\u5316","text":"<pre><code># optimizations/pooling.py\nfrom typing import Dict, Any\nfrom sqlalchemy.pool import QueuePool, NullPool, StaticPool\n\nclass PoolOptimizer:\n    \"\"\"\u30d7\u30fc\u30eb\u6700\u9069\u5316\u30d8\u30eb\u30d1\u30fc\"\"\"\n\n    @staticmethod\n    def get_optimal_pool_class(dialect: str, is_async: bool = False):\n        \"\"\"\u65b9\u8a00\u306b\u6700\u9069\u306a\u30d7\u30fc\u30eb\u30af\u30e9\u30b9\u3092\u9078\u629e\"\"\"\n\n        if dialect == \"sqlite\":\n            # SQLite \u306f\u30b7\u30f3\u30b0\u30eb\u30b9\u30ec\u30c3\u30c9\u306a\u306e\u3067 StaticPool\n            return StaticPool\n        elif is_async:\n            # \u975e\u540c\u671f\u306f\u901a\u5e38 NullPool \u304c\u63a8\u5968\n            return NullPool\n        else:\n            # \u305d\u306e\u4ed6\u306f QueuePool\n            return QueuePool\n\n    @staticmethod\n    def get_optimal_pool_size(dialect: str, expected_load: str = \"medium\") -&gt; Dict[str, int]:\n        \"\"\"\u8ca0\u8377\u306b\u5fdc\u3058\u305f\u6700\u9069\u306a\u30d7\u30fc\u30eb\u30b5\u30a4\u30ba\"\"\"\n\n        configs = {\n            \"low\": {\"pool_size\": 5, \"max_overflow\": 5},\n            \"medium\": {\"pool_size\": 10, \"max_overflow\": 10},\n            \"high\": {\"pool_size\": 20, \"max_overflow\": 20},\n            \"extreme\": {\"pool_size\": 50, \"max_overflow\": 50}\n        }\n\n        base_config = configs.get(expected_load, configs[\"medium\"])\n\n        # \u65b9\u8a00\u56fa\u6709\u306e\u8abf\u6574\n        if dialect == \"oracle\":\n            # Oracle \u306f\u63a5\u7d9a\u30b3\u30b9\u30c8\u304c\u9ad8\u3044\u306e\u3067\u30d7\u30fc\u30eb\u30b5\u30a4\u30ba\u3092\u5897\u3084\u3059\n            base_config[\"pool_size\"] = int(base_config[\"pool_size\"] * 1.5)\n        elif dialect == \"sqlite\":\n            # SQLite \u306f\u63a5\u7d9a\u304c\u8efd\u91cf\n            base_config[\"pool_size\"] = 1\n            base_config[\"max_overflow\"] = 0\n\n        return base_config\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#82","title":"8.2 \u30af\u30a8\u30ea\u6700\u9069\u5316","text":"<pre><code># optimizations/query.py\nfrom typing import List, Any\nfrom sqlalchemy import text\nfrom sqlalchemy.sql import Select\n\nclass QueryOptimizer:\n    \"\"\"\u30af\u30a8\u30ea\u6700\u9069\u5316\u30d8\u30eb\u30d1\u30fc\"\"\"\n\n    @staticmethod\n    def add_execution_options(stmt: Select, dialect: str) -&gt; Select:\n        \"\"\"\u65b9\u8a00\u56fa\u6709\u306e\u5b9f\u884c\u30aa\u30d7\u30b7\u30e7\u30f3\u8ffd\u52a0\"\"\"\n\n        if dialect == \"postgresql\":\n            # PostgreSQL \u56fa\u6709\u306e\u6700\u9069\u5316\n            return stmt.execution_options(\n                postgresql_readonly=True,\n                postgresql_deferrable=True\n            )\n        elif dialect == \"mysql\":\n            # MySQL \u56fa\u6709\u306e\u6700\u9069\u5316\n            return stmt.execution_options(\n                mysql_buffered=True\n            )\n\n        return stmt\n\n    @staticmethod\n    def optimize_bulk_insert(\n        values: List[Dict[str, Any]],\n        dialect: str\n    ) -&gt; Dict[str, Any]:\n        \"\"\"\u30d0\u30eb\u30af\u30a4\u30f3\u30b5\u30fc\u30c8\u6700\u9069\u5316\"\"\"\n\n        if dialect == \"postgresql\":\n            # PostgreSQL \u306f COPY \u304c\u6700\u901f\n            return {\n                \"method\": \"copy\",\n                \"chunk_size\": 10000\n            }\n        elif dialect == \"mysql\":\n            # MySQL \u306f multi-value insert\n            return {\n                \"method\": \"multi\",\n                \"chunk_size\": 5000\n            }\n        else:\n            # \u30c7\u30d5\u30a9\u30eb\u30c8\n            return {\n                \"method\": \"multi\",\n                \"chunk_size\": 1000\n            }\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#9","title":"9. \u30bb\u30ad\u30e5\u30ea\u30c6\u30a3\u8003\u616e\u4e8b\u9805","text":""},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#91","title":"9.1 \u8a8d\u8a3c\u60c5\u5831\u306e\u4fdd\u8b77","text":"<pre><code># security/credentials.py\nimport os\nfrom typing import Optional\nfrom cryptography.fernet import Fernet\n\nclass CredentialManager:\n    \"\"\"\u8a8d\u8a3c\u60c5\u5831\u7ba1\u7406\"\"\"\n\n    def __init__(self, key: Optional[bytes] = None):\n        if key is None:\n            key = os.getenv(\"UNICONNDB_ENCRYPTION_KEY\")\n            if not key:\n                key = Fernet.generate_key()\n\n        self.cipher = Fernet(key)\n\n    def encrypt_password(self, password: str) -&gt; str:\n        \"\"\"\u30d1\u30b9\u30ef\u30fc\u30c9\u6697\u53f7\u5316\"\"\"\n        return self.cipher.encrypt(password.encode()).decode()\n\n    def decrypt_password(self, encrypted: str) -&gt; str:\n        \"\"\"\u30d1\u30b9\u30ef\u30fc\u30c9\u5fa9\u53f7\"\"\"\n        return self.cipher.decrypt(encrypted.encode()).decode()\n\n    @staticmethod\n    def mask_url(url: str) -&gt; str:\n        \"\"\"URL \u306e\u30d1\u30b9\u30ef\u30fc\u30c9\u90e8\u5206\u3092\u30de\u30b9\u30af\"\"\"\n        from urllib.parse import urlparse, urlunparse\n\n        parsed = urlparse(url)\n        if parsed.password:\n            # \u30d1\u30b9\u30ef\u30fc\u30c9\u3092 *** \u306b\u7f6e\u63db\n            masked_netloc = parsed.netloc.replace(\n                f\":{parsed.password}@\",\n                \":***@\"\n            )\n            masked = parsed._replace(netloc=masked_netloc)\n            return urlunparse(masked)\n\n        return url\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#92-sql","title":"9.2 SQL \u30a4\u30f3\u30b8\u30a7\u30af\u30b7\u30e7\u30f3\u5bfe\u7b56","text":"<pre><code># security/validation.py\nimport re\nfrom typing import Any\n\nclass SQLValidator:\n    \"\"\"SQL \u30d0\u30ea\u30c7\u30fc\u30b7\u30e7\u30f3\"\"\"\n\n    # \u5371\u967a\u306a\u30d1\u30bf\u30fc\u30f3\n    DANGEROUS_PATTERNS = [\n        r\";\\s*(DROP|DELETE|TRUNCATE|UPDATE|INSERT)\",\n        r\"--\",\n        r\"/\\*.*\\*/\",\n        r\"xp_cmdshell\",\n        r\"exec\\s*\\(\",\n        r\"UNION\\s+SELECT\"\n    ]\n\n    @classmethod\n    def is_safe_identifier(cls, identifier: str) -&gt; bool:\n        \"\"\"\u8b58\u5225\u5b50\u306e\u5b89\u5168\u6027\u30c1\u30a7\u30c3\u30af\"\"\"\n        # \u82f1\u6570\u5b57\u3068\u30a2\u30f3\u30c0\u30fc\u30b9\u30b3\u30a2\u306e\u307f\u8a31\u53ef\n        return bool(re.match(r\"^[a-zA-Z_][a-zA-Z0-9_]*$\", identifier))\n\n    @classmethod\n    def validate_query(cls, query: str) -&gt; bool:\n        \"\"\"\u30af\u30a8\u30ea\u306e\u5371\u967a\u6027\u30c1\u30a7\u30c3\u30af\"\"\"\n        for pattern in cls.DANGEROUS_PATTERNS:\n            if re.search(pattern, query, re.IGNORECASE):\n                return False\n        return True\n\n    @classmethod\n    def escape_identifier(cls, identifier: str, dialect: str) -&gt; str:\n        \"\"\"\u8b58\u5225\u5b50\u306e\u30a8\u30b9\u30b1\u30fc\u30d7\"\"\"\n        if dialect == \"postgresql\":\n            return f'\"{identifier}\"'\n        elif dialect == \"mysql\":\n            return f\"`{identifier}`\"\n        elif dialect == \"mssql\":\n            return f\"[{identifier}]\"\n        else:\n            return identifier\n</code></pre>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#10","title":"10. \u5b9f\u88c5\u30b9\u30b1\u30b8\u30e5\u30fc\u30eb","text":""},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#1","title":"\u30d5\u30a7\u30fc\u30ba 1: \u57fa\u76e4\u5b9f\u88c5","text":"<ol> <li>\u30d7\u30ed\u30b8\u30a7\u30af\u30c8\u69cb\u9020\u30bb\u30c3\u30c8\u30a2\u30c3\u30d7</li> <li><code>DBConfig</code> \u3068\u57fa\u672c\u30c7\u30fc\u30bf\u69cb\u9020</li> <li><code>DriverRegistry</code> \u5b9f\u88c5</li> <li>\u57fa\u672c\u7684\u306a <code>DatabaseManager</code> \u5b9f\u88c5</li> </ol>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#2","title":"\u30d5\u30a7\u30fc\u30ba 2: \u6a5f\u80fd\u62e1\u5f35","text":"<ol> <li><code>ConnectionDiscovery</code> \u5b9f\u88c5</li> <li><code>AsyncDatabaseManager</code> \u5b9f\u88c5</li> <li>\u8a8d\u8a3c\u30d7\u30e9\u30b0\u30a4\u30f3\u30b7\u30b9\u30c6\u30e0</li> <li>CRUD \u30e6\u30fc\u30c6\u30a3\u30ea\u30c6\u30a3</li> </ol>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#3","title":"\u30d5\u30a7\u30fc\u30ba 3: \u54c1\u8cea\u4fdd\u8a3c","text":"<ol> <li>\u30e6\u30cb\u30c3\u30c8\u30c6\u30b9\u30c8\u4f5c\u6210</li> <li>\u7d71\u5408\u30c6\u30b9\u30c8\u74b0\u5883\u69cb\u7bc9</li> <li>\u30c9\u30ad\u30e5\u30e1\u30f3\u30c8\u4f5c\u6210</li> <li>CI/CD \u30d1\u30a4\u30d7\u30e9\u30a4\u30f3\u8a2d\u5b9a</li> </ol>"},{"location":"design/%E8%A9%B3%E7%B4%B0%E8%A8%AD%E8%A8%88%E6%9B%B8/#4","title":"\u30d5\u30a7\u30fc\u30ba 4: \u6700\u9069\u5316\u3068\u516c\u958b","text":"<ol> <li>\u30d1\u30d5\u30a9\u30fc\u30de\u30f3\u30b9\u6700\u9069\u5316</li> <li>\u30bb\u30ad\u30e5\u30ea\u30c6\u30a3\u5f37\u5316</li> <li>\u30b5\u30f3\u30d7\u30eb\u30b3\u30fc\u30c9\u4f5c\u6210</li> <li>PyPI \u516c\u958b\u6e96\u5099</li> </ol> <p>\u3053\u306e\u8a73\u7d30\u8a2d\u8a08\u66f8\u306b\u3088\u308a\u3001UniConnDBBridge \u306e\u5b9f\u88c5\u306b\u5fc5\u8981\u306a\u3059\u3079\u3066\u306e\u6280\u8853\u7684\u8a73\u7d30\u304c\u5b9a\u7fa9\u3055\u308c\u307e\u3057\u305f\u3002\u5404\u30b3\u30f3\u30dd\u30fc\u30cd\u30f3\u30c8\u306f\u758e\u7d50\u5408\u3067\u3001\u30c6\u30b9\u30c8\u53ef\u80fd\u3067\u3001\u62e1\u5f35\u53ef\u80fd\u306a\u8a2d\u8a08\u3068\u306a\u3063\u3066\u3044\u307e\u3059\u3002</p>"}]}